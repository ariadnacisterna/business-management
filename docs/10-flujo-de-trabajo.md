# Flujo de trabajo entre conversaciones

Este documento define cómo se organiza la programación del sistema entre
varias conversaciones de un asistente de IA, para sostener el proyecto en el
tiempo sin que una única conversación crezca tanto que pierda precisión, y sin
gastar en un modelo costoso lo que puede resolver uno más liviano.

## Idea general

- **Conversación madre:** sostiene el estado del proyecto. Aprueba decisiones,
  revisa el trabajo terminado y decide si está listo para integrarse a
  `main`. No implementa tareas grandes por sí misma.
- **Conversación de tarea:** conversación nueva y acotada a una sola tarea.
  Parte de esta documentación —no necesita el historial de la madre— y termina
  dejando los cambios listos para revisar.

Esto funciona porque los documentos 01 a 09 son una especificación
autosuficiente (D-025): cualquier conversación nueva tiene ahí todo lo que
necesita para implementar una tarea sin que nadie se la explique de nuevo.

## Los comandos de git son siempre manuales

Ninguna conversación de IA —ni la madre ni una de tarea— ejecuta un comando
de git que modifique el repositorio, su índice o su historial, en ningún
caso: ni `git commit`, `git merge`, `git push`, ni tampoco `git add` ni la
creación de ramas. Es el Responsable del proyecto quien decide qué queda
escrito en el repositorio y quién lo escribe.

Una conversación de IA puede:

- modificar archivos, sin agregarlos al índice de git;
- redactar el mensaje de commit sugerido, para copiar y pegar;
- leer el estado del repositorio con comandos que no lo modifican, como
  `git status`, `git log`, `git diff` o `git show`.

Una conversación de IA no ejecuta, bajo ninguna circunstancia, ningún
comando que cambie el repositorio —tampoco `git add` ni crear una rama—
aunque el pedido del Responsable del proyecto parezca implicar permiso
("fijate si quedó bien", "subilo", "dejalo preparado"). Si hace falta alguno
de esos pasos, se lo pide de forma explícita e inequívoca en ese momento
puntual, y aun así esta regla del proyecto prevalece: el paso lo ejecuta el
Responsable.

## Formato del mensaje de commit sugerido

Toda conversación redacta el mensaje que sugiere siguiendo **Conventional
Commits**, en inglés (mismo criterio que el código, D-031), **en una sola
línea, sin cuerpo**: `<tipo>(<id de tarea>): <descripción breve, en
imperativo>`.

Tipos más usados en este proyecto:

| Tipo | Cuándo |
|---|---|
| `feat:` | una funcionalidad nueva |
| `fix:` | corrige un comportamiento incorrecto |
| `refactor:` | cambia la estructura interna sin cambiar el comportamiento |
| `test:` | agrega o modifica pruebas |
| `docs:` | cambios en `docs/` |
| `chore:` | mantenimiento: dependencias, configuración, migraciones |

Ejemplo:

```
feat(T-042): add login help modal for password reset guidance
```

## Convención de ramas

- Cada tarea vive en su propia rama, con nombre en inglés como el resto del
  código (D-031): `task/<id>-<short-description>`, por ejemplo
  `task/T-003-add-product`. La crea siempre el Responsable del proyecto
  antes de empezar; una conversación de IA nunca la crea por su cuenta.
- Al terminar, la conversación de tarea deja: los archivos modificados sin
  agregar al índice de git, el mensaje de commit sugerido (una sola línea,
  ver formato abajo), los comandos que el Responsable tiene que correr para
  verificar (backend y frontend, los que apliquen: típicamente `pytest`,
  `ruff` del lado backend y `npm run typecheck`/`npm run lint`/`npm run
  test`/`npm run build` del lado frontend), y su fila actualizada en el
  [registro de tareas](#registro-de-tareas) más abajo, con estado `Lista
  para revisión`.
- Una conversación de tarea escribe en la columna "Resultado de
  verificación" únicamente su propio resumen (qué hizo, qué pruebas corrió,
  qué ambigüedades resolvió). Nunca escribe un veredicto ni las palabras
  "Aprobada", "Verificada" ni "Revisado por la madre": eso lo determina
  exclusivamente la conversación madre, después de revisar por su cuenta —
  no antes, y no aunque la propia tarea esté segura de haber hecho todo
  bien.

## Plantilla para abrir una conversación de tarea

Copiar y completar al iniciar una conversación nueva:

```
Estoy trabajando en un proyecto de gestión de negocio (catálogo y precios,
con el resto del alcance en docs/01-vision-y-alcance.md) documentado en
docs/. El primer negocio real es una mercería y librería, pero el sistema
está pensado para adaptarse a otros rubros sin cambiar el código (D-023):
evitá cualquier supuesto específico de mercería fuera de los datos de
ejemplo.
Quiero que implementes la tarea T-XXX: <descripción corta>.

Leé antes de empezar:
- docs/09-propuesta-arquitectura-tecnica.md (arquitectura aprobada)
- docs/07-modelo-conceptual.md (modelo de datos)
- Los requisitos y reglas puntuales: <RF-XXX, RN-XXX, HU-XXX>

Alcance de esta tarea: <qué entra y qué explícitamente no entra>

Instrucciones:
1. Trabajá sobre la rama task/T-XXX-<description> (ya creada por mí). Si no
   existe todavía, pedime que la cree antes de avanzar; vos no la crees.
2. Implementá solo lo necesario para esta tarea.
3. Agregá pruebas cuando corresponda.
4. Dejá los archivos modificados sin agregarlos al índice de git (sin `git
   add`) y redactá el mensaje de commit sugerido en una sola línea, formato
   Conventional Commits con el id de tarea (`feat(T-XXX): ...`, ver
   docs/10-flujo-de-trabajo.md), sin cuerpo. No ejecutes ningún comando de
   git que modifique el repositorio —tampoco `git add`— bajo ninguna
   circunstancia: esos pasos los hago yo. Podés usar git solo para leer
   (`git status`, `git diff`, `git log`).
5. Al terminar, actualizá tu fila en la tabla de docs/10-flujo-de-trabajo.md
   con estado "Lista para revisión", el mensaje de commit sugerido, un
   resumen breve de qué archivos tocaste y qué quedó pendiente, si algo, y
   los comandos que tengo que correr para verificar (backend y frontend,
   los que apliquen). No escribas un veredicto ni las palabras "Aprobada",
   "Verificada" ni "Revisado por la madre": eso lo decide únicamente la
   conversación madre, después de revisar por su cuenta.

No hace falta que expliques cada paso intermedio; al final alcanza con el
resumen del punto 5.
```

## Checklist de verificación (conversación madre)

Para cada tarea en estado `Lista para revisión`:

1. Leer el diff de la rama contra `main`.
2. Confirmar que solo tocó lo que la tarea pedía.
3. Contrastar el código contra los RF, RN, CU o HU referenciados — que cumpla
   la regla realmente, no solo que el nombre de una función lo sugiera.
4. Correr las pruebas si existen.
5. Aplicar además el checklist de seguridad de
   [11-seguridad-y-privacidad.md](11-seguridad-y-privacidad.md#checklist-de-seguridad-para-la-conversación-madre)
   y el de [12-estandares-de-codigo.md](12-estandares-de-codigo.md#verificación).
6. Si está bien: avisar al Responsable del proyecto que la tarea está lista
   para commitear y mergear a `main`, con el mensaje de commit sugerido. La
   madre marca la tarea `Verificada` con la fecha una vez que el Responsable
   confirma que lo hizo.
7. Si falta algo: dejarla en `Necesita ajustes` con el detalle puntual, para
   resolverlo en una conversación nueva o en la misma.

## Qué modelo usar en cada conversación

- **Madre:** el modelo de mayor capacidad de razonamiento disponible, porque
  decide arquitectura y es la última revisión antes de integrar código.
- **Tarea:** alcanza con un modelo más liviano o el modo rápido cuando la
  tarea está bien acotada y la especificación es explícita. Reservar el modelo
  más capaz para tareas ambiguas o de alto riesgo: transacciones de precio,
  permisos, migraciones.

## Registro de tareas

Tabla viva. Cada conversación de tarea agrega o actualiza su propia fila; la
madre la revisa y cierra el estado.

| ID | Descripción | Requisitos | Rama | Estado | Resultado de verificación |
|---|---|---|---|---|---|
| T-001 | Esqueleto del proyecto: estructura FastAPI, configuración, primera migración de Alembic (organización, negocio, rol, usuario) | D-026, D-017, D-018 | task/T-001-project-skeleton | Verificada (2026-08-28) | Revisado por la madre: checklist de 10/11/12 aplicado, migración y pruebas corridas contra PostgreSQL real de forma independiente. Se corrigieron los comentarios autogenerados por Alembic. Mergeada a main mediante PR #1 (commit 10f6061). |
| T-002 | Autenticación y sesión: login, logout, cuenta activa, alta y gestión de cuentas de usuario, rol Gerente (D-035) | RF-024, RF-025, RF-031, CU-03, HU-06, RN-009, RN-032 | task/T-002-authentication-and-accounts | Verificada (2026-08-28) | Revisado por la madre: se corrigió un bug bloqueante (import de roles roto), se completó el pedido de identificadores en inglés y el rol Gerente, checklist de 10/11/12 aplicado, 25 pruebas corridas contra PostgreSQL real de forma independiente. Mergeada a main (commit 3a9b847). Pendiente no bloqueante: el token de sesión se guarda sin hashear como clave primaria de `sesion` (a diferencia de la contraseña, RNF-013); queda anotado para revisar más adelante. |
| T-003 | Catálogo: categorías, unidades, atributos y valores, productos y variantes (alta y edición) | RF-010 a RF-017, RF-032 a RF-037, RF-040, CU-04, CU-05, CU-08, HU-04, HU-09 | task/T-003-catalog | Verificada (2026-09-02) | Revisado por la madre: checklist de 10/11/12 aplicado, 43 pruebas corridas contra PostgreSQL real de forma independiente, sin drift de `alembic check`. Mergeada a main (commit 9740103). Encontré y corregí tres cosas antes de aprobar: (1) faltaba actor/fecha en toda operación de catálogo pese a que HU-04 lo exige — agregué `created_by_account_id`/`created_at`/`updated_by_account_id`/`updated_at` (mixin `AuditedMixin`) a las 6 tablas nuevas y los propagué por dominio y API; (2) `add_variant` convertía la variante implícita en "real" sin pedirle nombre, dejando una variante sin forma de distinguirla — ahora exige nombrarla primero con `update_variant`; (3) al corregir lo anterior encontré un bug real de caché de SQLAlchemy (`product.variants` quedaba desactualizado dentro de la misma sesión tras crear una variante) y lo arreglé con `db.expire`. Reescribí el test afectado y agregué cobertura para los tres casos. Corregí también un detalle de formato (`ruff format`) y un acento faltante en los valores de color sembrados ("Marrón"). |
| T-004 | Precios: precio vigente, cambio transaccional individual y por producto, historial | RF-018 a RF-023, RF-027, RF-038, CU-06, CU-07, HU-05, HU-10 | task/T-004-pricing | Verificada (2026-09-02) | Revisado por la madre (después de mergeada — ver nota): checklist de 10/11/12 aplicado, 61 pruebas re-corridas de forma independiente contra PostgreSQL real, sin drift de `alembic check`. Sin correcciones necesarias; confirmo las tres ambigüedades anotadas abajo como resoluciones razonables. Mergeada a main (commit 71472be). Modelo `Price` (`backend/app/db/models/price.py`) con `amount` decimal exacto (`Numeric(12,2)`, nunca float), `effective_from`/`effective_to` y `created_by_account_id`/`created_at` (sin `updated_*`: un precio nunca se edita, ver nota de ambigüedad). Restricciones en PostgreSQL: `CHECK (amount > 0)` e índice único parcial sobre `(variant_id, business_id)` donde `effective_to IS NULL`, ambas ejercitadas contra PostgreSQL real (no solo validadas por la API). Migración `6a07f9b29396_add_pricing.py`, sin drift de `alembic check`. Dominio `backend/app/domain/pricing/{prices.py,errors.py}`: `change_variant_price` (RF-018 a RF-021, RN-003 a RN-006) y `change_product_price` (RF-038, aplica a todas las variantes activas del producto) comparten la misma rutina interna de cierre+alta (`_apply_price_change`), aplicada en bucle dentro de una sola transacción para el caso por producto (D-031, sin reimplementar la lógica). Conflicto optimista (CU-06 A2, RF-027): cada cambio recibe el id del precio vigente que la usuaria vio (`null` si no vio ninguno); si no coincide con el vigente real, se rechaza con 409 y el valor actual en vez de aplicarlo a ciegas; para el cambio por producto la detección considera el conjunto y devuelve el estado de todas las variantes activas (CU-06 A3). Atomicidad verificada con un caso que fuerza una falla a mitad del bucle (segunda variante) y confirma que ninguna queda con el precio nuevo, además del caso natural donde una variante desactualizada rechaza la operación completa. Endpoints en `backend/app/api/pricing.py`: `GET/PUT /variants/{id}/price` (vigente/cambio), `GET /variants/{id}/prices` (historial de solo lectura, RF-022, RF-023, CU-07), `PUT /products/{id}/price` (cambio por producto); lectura con `get_current_user` (RN-009, cualquier rol autenticado), cambios con `require_role(Administrador, Gerente)` (RN-032). 18 pruebas nuevas en `backend/tests/test_pricing.py` corridas contra PostgreSQL real (contenedor Docker efímero, sin `docker-compose` en el repo) junto con las 43 previas, 61 en total, sin regresiones. Ambigüedades menores resueltas y anotadas para revisión: (1) `AuditedMixin` no se reutilizó tal cual porque exige `updated_by`/`updated_at` NOT NULL, que el precio no necesita (RF-023); en su lugar declaré `created_by_account_id`/`created_at` directamente en el modelo, mismo patrón que ya usa `AccountSession`. (2) El "precio que la usuaria vio" (arquitectura, sección Cambio de precio) lo interpreté como el id de fila del precio vigente que el cliente consultó por última vez (no el importe), porque dos cambios distintos podrían coincidir en el mismo importe sin ser el mismo evento. (3) No agregué columna de moneda: RN-023 fija ARS para todo el sistema, no varía por registro. Pendiente no bloqueante: al escribir las pruebas encontré que `TestClient` (httpx) persiste cookies entre pedidos del mismo cliente; una prueba que primero inicia sesión y luego simula "sin sesión" debe limpiar `client.cookies` explícitamente o el pedido queda autenticado igual — no es un bug de producción, solo una trampa a tener en cuenta al escribir pruebas nuevas. |
| T-005 | Búsqueda y consulta | RF-002 a RF-009, RF-035, CU-01, CU-02, HU-01, HU-02, HU-03 | task/T-005-search | Verificada (2026-09-02) | Revisado por la madre: checklist de 10/11/12 aplicado, 72 pruebas re-corridas de forma independiente contra PostgreSQL real, sin observaciones de `ruff`. Mergeada a main (commit 10cfd1b). Coincido con las cuatro ambigüedades resueltas abajo, incluida la decisión de filtrar en Python en vez de SQL — la evalué por mi cuenta antes de leer la justificación y llegué a la misma conclusión (doc09 no pide optimizar sin evidencia, y la escala actual no lo amerita). Resumen de la tarea: endpoint `GET /search` (`backend/app/api/search.py`), lectura con `get_current_user` (RN-009, cualquier rol autenticado); parámetros de consulta `q` (términos libres, opcional) y `category_id` (opcional). Dominio `backend/app/domain/catalog/search.py`, función `search_variants`: filtra variantes activas de productos activos de la organización (RF-009), opcionalmente por categoría (RF-006, valida que exista y responde 404 si no), aplica semántica `AND` entre términos (doc09) sobre un texto derivado del nombre de producto, la categoría, el label de la variante y sus valores de atributo, normalizado con el `normalize_for_comparison` ya existente de T-003 (RF-004, RF-005, D-031: no se reimplementa), y descarta las variantes sin precio vigente en el negocio activo antes de responder (RF-035, RN-028). Agregué `get_current_prices_for_variants` (búsqueda en lote) en `backend/app/domain/pricing/prices.py`, con `_get_current_price` delegando en ella, para no duplicar el filtro de "precio vigente" que ya usa T-004 (D-031). Cada resultado trae `variant_id`, `product_id`, `product_name`, `category_id`, `category_name`, `label`, `attribute_values` (atributo y valor) y `price_amount`, lo mínimo para distinguir resultados (RF-007); sin coincidencias responde 200 con lista vacía, no un error (RF-008). Devuelve una fila por variante, sin agrupar por producto ni resolver DP-006 (queda para T-008, como indica doc09). 11 pruebas nuevas en `backend/tests/test_search.py`: mayúsculas/acentos distintos, orden de términos distinto, término sin coincidencias, variante sin precio vigente excluida, filtro por categoría, categoría combinada con término, producto inactivo excluido, categoría inexistente (404), Empleado puede buscar (RN-009), sin sesión (401), búsqueda sin términos lista todo lo disponible. 72 pruebas en total (61 previas + 11 nuevas) corridas contra PostgreSQL real (contenedor Docker efímero, puerto 5433 para evitar un conflicto con un servicio de PostgreSQL nativo en el 5432 y con el rango de puertos excluidos de Windows que bloqueaba el 55432 configurado en `.env`), sin regresiones; `alembic check` sin drift (esta tarea no modifica el esquema, no agrega migración); `ruff check` y `ruff format` sin observaciones. Ambigüedades menores resueltas, para que la madre las revise: (1) forma de la búsqueda: en vez de agregar columnas de texto normalizado a `Product`/`Category`/`Variant.label` (como `AttributeValue.normalized_value` de T-003), traigo las variantes candidatas ya acotadas por SQL (activas, de la organización, filtradas por categoría si corresponde) y aplico el filtro `AND` de términos en Python sobre texto normalizado en el momento; evita una migración y tocar los flujos de escritura del catálogo (fuera de alcance) y no reimplementa la normalización en SQL sin la extensión `unaccent` (no habilitada); doc09 deja la forma exacta pendiente de la muestra real, y a esta escala no debería ser un problema, pero si la muestra real lo exige, columnas normalizadas o `unaccent` son el paso siguiente natural. (2) Qué cuenta como "características" buscables (RF-003): sumé `variant.label` a nombre, categoría y atributos normalizados, porque es el texto libre que el dominio ya usa para distinguir variantes sin atributo formal (ejemplo "1.5 litros" de la gaseosa en doc04) y que RN-030 describe como texto libre; sin esto no sería buscable por ese texto. (3) Categoría inexistente en el filtro: devuelve 404 (mismo criterio que `GET /categories/{id}`) para distinguir "filtro inválido" de "sin coincidencias" (RF-008). (4) No agregué paginación: no la piden los RF y evita complejidad prematura; si la muestra real la necesita, es un cambio acotado al endpoint. Sin pendientes no bloqueantes detectados. |
| T-006 | Desactivación y reactivación de productos y variantes | RF-016, CU-10, HU-08 | task/T-006-deactivation | Verificada (2026-09-03) | Revisado por la madre: checklist de 10/11/12 aplicado, 79 pruebas re-corridas de forma independiente contra PostgreSQL real, sin drift de `alembic check`, sin observaciones de `ruff`. Sin correcciones; coincido con las tres ambigüedades resueltas abajo. Mergeada a main (commit de72f2e). Mensaje de commit sugerido: `feat(T-006): add deactivation and reactivation for products and variants` / cuerpo: `Implements RF-016, RN-011, RN-012, CU-10, HU-08 (T-006).`. No hizo falta migración: se reutilizan `status` y `updated_by_account_id`/`updated_at` (`AuditedMixin`) que `Product` y `Variant` ya tienen desde T-003. Agregué a `backend/app/domain/catalog/products.py` cuatro funciones (`deactivate_product`, `reactivate_product`, `deactivate_variant`, `reactivate_variant`) que reutilizan `get_product`/`get_variant` existentes y comparten un helper privado `_set_status` para no repetir la asignación de `status`/`updated_by_account_id`/`updated_at` cuatro veces. Cuatro endpoints nuevos en `backend/app/api/catalog.py`: `POST /products/{id}/deactivate`, `POST /products/{id}/reactivate`, `POST /variants/{id}/deactivate`, `POST /variants/{id}/reactivate`, todos con `require_role(ADMINISTRADOR, GERENTE)` (RN-032, mismo criterio que el resto de T-003) y `require_csrf`; devuelven 404 (`ProductNotFound`/`VariantNotFound`) si no existen. No reimplementé la cascada de CU-10 ("desactivar un producto retira también sus variantes de las consultas"): ya está resuelta por el filtro doble de T-005 (`backend/app/domain/catalog/search.py`, `Product.status == ACTIVE` Y `Variant.status == ACTIVE`), así que desactivar el producto no toca el `status` de sus variantes. 7 pruebas nuevas en `backend/tests/test_catalog.py`: desactivar producto oculta sus variantes de `/search` sin cambiar el `status` individual de cada variante, reactivar producto lo vuelve a mostrar en la búsqueda, desactivar una variante no afecta a las demás del mismo producto, reactivar una variante, Gerente puede desactivar y reactivar un producto (RN-032), Empleado no puede desactivar ni un producto ni una variante (403, RN-009), y que la desactivación deja registrados `updated_by_account_id`/`updated_at` del actor. 79 pruebas en total (72 previas + 7 nuevas) corridas contra PostgreSQL real (contenedor Docker efímero, puerto 5433, mismo criterio que T-005), sin regresiones; `alembic check` sin drift (no hay migración nueva); `ruff check` y `ruff format` sin observaciones. Ambigüedades menores resueltas, para que la madre las revise: (1) forma de los endpoints: elegí acciones explícitas `POST .../deactivate` y `POST .../reactivate` en vez de agregar `status` como campo editable a `PATCH /products/{id}` o `PATCH /variants/{id}`, porque CU-10 describe un paso explícito de "solicita cambiar el estado" con confirmación propia, separado de la edición de nombre/categoría/unidad/label/atributos que ya cubren esos PATCH; también evita mezclar semánticas (un PATCH parcial con `status` se presta a activar/desactivar como efecto secundario de una edición no relacionada). (2) Idempotencia: desactivar un producto o variante ya inactivo (o reactivar uno ya activo) no lanza error, simplemente vuelve a fijar el estado y actualiza `updated_by_account_id`/`updated_at`; ningún documento pide bloquear la operación en ese caso y tratarla como no-op parece la resolución más simple. (3) La respuesta de los endpoints de variante devuelve `VariantResponse` (no `VariantCreationResponse` con `possible_duplicates`) porque desactivar/reactivar no cambia `label` ni atributos, así que no corresponde volver a evaluar duplicados. Sin pendientes no bloqueantes detectados. |
| T-007 | Importación por CSV/Excel | RF-028 a RF-030, CU-09, HU-07 | task/T-007-import | Verificada (2026-09-03) | Revisado por la madre: checklist de 10/11/12 aplicado, 105 pruebas de la tarea re-corridas de forma independiente contra PostgreSQL real, sin drift de `alembic check`. Coincido con el punto de diseño que la propia tarea marcó para mi atención (extraer versiones privadas sin `commit()` de `create_category`, `create_unit`, `create_attribute_value`, `create_product` y `add_variant` para lograr atomicidad real): lo revisé contra las 79 pruebas previas de catalog, que siguen pasando sin cambios, y es el mismo patrón ya validado en `_apply_price_change` desde T-004. Encontré y corregí dos cosas antes de aprobar: (1) `_parse_csv` decodificaba el archivo únicamente como UTF-8 (`utf-8-sig`); un CSV exportado desde Excel en Windows en español (el caso normal para este usuario, no el excepcional) suele usar CP-1252, así que cualquier acento o "ñ" en un nombre de producto o categoría rompía la carga con un error 500 sin mensaje, en vez del error comprensible que exige RN-014/HU-07 — agregué un intento de reintento con CP-1252 y, si tampoco decodifica, un error 422 explícito pidiendo volver a guardar como "CSV UTF-8"; agregué dos pruebas (`test_cp1252_encoded_csv_from_windows_excel_is_accepted`, `test_csv_with_unrecognizable_encoding_is_rejected_with_a_clear_message`), total 107 pruebas. (2) `DEFAULT_UNIT_ALLOWS_FRACTION` estaba declarada como constante suelta en `applying.py`, la única constante de dominio del proyecto fuera de `app/constants/` — la moví a `constants/import_.py` (doc12, "Sin valores hardcodeados"), sin cambio de comportamiento. `ruff check`, `ruff format` y `alembic check` sin observaciones después de mis cambios. Mergeada a main (commits 54a0abd y e96b1c6). Sin otras correcciones; coincido con las demás resoluciones de la tarea (columna `attributes` con formato `atributo=valor`, "todo o nada" según D-036, duplicados vía `find_possible_duplicates` sin búsqueda difusa, límites de tamaño/filas, solo `.xlsx`). Mensaje de commit sugerido para mis dos ajustes: `fix(T-007): accept CP-1252 CSV encoding and move import default to constants` / cuerpo: `Fixes a raw 500 on Windows-Excel CSV exports with accents and centralizes DEFAULT_UNIT_ALLOWS_FRACTION per doc12 (T-007 madre review).` — a commitear como commit separado del de la tarea, o a integrar como el Responsable prefiera. Mensaje de commit original de la tarea, sin cambios: `feat(T-007): add catalog import from CSV/Excel` / cuerpo: `Implements RF-028, RF-029, RF-030, RN-013, RN-014, RN-015, RN-020, RN-024, RN-026, RN-027, RN-032, CU-09, HU-07 (T-007).`. Dos endpoints en `backend/app/api/imports.py`: `POST /imports/preview` (analiza sin escribir, RN-013) y `POST /imports` (confirma, atómico), ambos con `require_role(ADMINISTRADOR)` (RN-032, a diferencia del resto del catálogo que también admite Gerente) y el segundo con `require_csrf`. Dominio nuevo `backend/app/domain/import_/{parsing,planning,applying,errors}.py`: `parsing.py` lee CSV (`csv.DictReader`) o Excel (`openpyxl`, agregado como dependencia nueva junto con `python-multipart` para el upload) según la extensión y valida columnas obligatorias (`category`, `product_name`, `unit`, `price`) y tamaño/cantidad de filas (límites nuevos en `constants/import_.py`); `planning.py` (`analyze_import`) agrupa filas por `category`+`product_name` (CU-09), clasifica cada fila como alta o actualización comparando contra el catálogo existente y contra lo que el propio archivo ya definió antes en sus filas anteriores, valida RN-024 (precio), RN-026 (variante implícita solo si el producto agrupa una fila), unidad consistente dentro de un mismo producto, atributo-tipo inexistente como error (alcance explícito de la tarea), valor de atributo inexistente como alta de taxonomía (RN-020), fila repetida dentro del archivo como error (no se reconcilia con "último cambio gana": RN-015 ya está cubierta por el resto del sistema y esta tarea no debía extender esa lógica), y detecta posibles duplicados reutilizando `find_possible_duplicates` sin modificarla; `applying.py` (`apply_import`) vuelve a llamar `analyze_import` con el mismo archivo antes de escribir (si hay errores no aplica nada, D-036) y aplica todo en una única transacción. Tabla y modelo nuevos `import_run` (migración `62f367492a3f_add_import.py`, sin drift de `alembic check`) para RF-030 (fecha y cuenta responsable, más un resumen de conteos). Punto de diseño para que la madre revise con atención: las funciones de dominio reutilizadas (`create_product`, `add_variant`, `create_category`, `create_unit`, `create_attribute_value`, `change_variant_price`) hacen su propio `commit()`, lo que hace imposible una transacción atómica de varias filas si se llaman tal cual (lo comprobé con un script antes de decidir el enfoque: un `db.commit()` interno no queda contenido por `db.begin_nested()` en SQLAlchemy). Por eso extraje de cada una una versión privada sin commit (`_build_category`, `_build_unit`, `_build_attribute_value`, `_create_product_core`, `_add_variant_core` en sus archivos originales de `catalog/`; `_apply_price_change` en `pricing/prices.py` ya existía con este mismo patrón desde T-004) y son esas versiones privadas las que reutiliza `applying.py`, con un único `commit()` final. Es un refactor mínimo y sin cambio de comportamiento (las 79 pruebas previas de catalog/pricing pasan sin modificarlas) pero toca cuatro archivos ya verificados de T-003, y el import cruza el guion bajo "privado" de un módulo a otro — lo marco explícitamente en vez de darlo por aceptado. Otras resoluciones menores para que la madre las revise: (1) la plantilla (PT-007) no tiene columna de abreviatura para `unit`, así que una unidad creada por importación usa su propio nombre como abreviatura y `allows_fraction=False` por defecto, editable después con los endpoints ya existentes; (2) "duplicados" en la vista previa sale de `find_possible_duplicates` tal cual existe (coincidencia exacta salvo mayúsculas/acentos con otro producto ya existente de igual nombre), no es una búsqueda difusa de errores de tipeo; (3) tamaño máximo de archivo 10 MB y 20.000 filas (`constants/import_.py`), sin exigencia explícita en los documentos pero razonable para un endpoint nuevo de carga de archivos; (4) solo `.xlsx` para Excel, no el formato binario `.xls` legado. 26 pruebas nuevas en `backend/tests/test_imports.py` (filas inválidas, fila repetida dentro del archivo, alta de categoría/unidad/valor de atributo inexistente, atributo-tipo inexistente como error, atomicidad forzando una fila inválida entre válidas y verificando que no se creó nada, reimportar el mismo archivo como actualización sin generar historial de precio si no cambió, dos productos nuevos que comparten taxonomía nueva la crean una sola vez, variante nueva sobre producto con variante implícita sin nombre como error, CSV y Excel, permisos de Administrador/Gerente/Empleado/anónimo, CSRF, columnas faltantes, extensión no admitida, archivo solo con encabezado) corridas junto con las 79 previas (105 en total) contra PostgreSQL real (contenedor Docker efímero, puerto 5433), sin regresiones; `ruff check` y `ruff format` sin observaciones; `alembic check` sin drift. No toqué SPA (T-008) ni la creación de atributos-tipo nuevos por importación (fuera de alcance explícito de la tarea). Sin pendientes no bloqueantes detectados más allá de los puntos de diseño ya anotados arriba. |
| T-008 | Interfaz (SPA): pantallas de las tareas anteriores | Todas las HU | — | Dividida en T-010 a T-015 (2026-09-03) | La madre dividió esta fila en tareas más chicas y verificables por separado: cubrir "todas las HU" de una sola vez era demasiado grande para una conversación de tarea y un commit revisables con rigor (nota al pie de este registro). Tecnología elegida: React + Vite (D-037). Ver T-010 a T-015. |
| T-009 | Renombrar a inglés los identificadores de código de T-001 ya mergeado (tablas, columnas, clases: Organizacion, Negocio, Rol, Usuario y sus campos), con la migración correspondiente | D-034 | task/T-009-rename-to-english | Verificada (2026-09-02) | Revisado por la madre: checklist de 10/11/12 aplicado, 26 pruebas corridas contra PostgreSQL real de forma independiente, incluida una prueba dedicada que corre la migración de renombrado sobre un esquema con datos existentes (no solo desde vacío) y su downgrade. Evitó los dos choques de nombre previsibles (usuario→"account", no "user"; sesión→"account_session", no "Session"). Mergeada a main (commit 12efd0a). Pendientes no bloqueantes: `ROLES_INICIALES`/`INITIAL_ROLES` se sigue importando en vivo desde la migración de T-001 en vez de usar valores literales (inconsistente con el resto de la migración, que sí quedó con literales); un `RuntimeError` interno sigue en español. |
| T-010 | SPA: esqueleto del proyecto (React + Vite), layout general e inicio/cierre de sesión | HU-06 | task/T-010-spa-skeleton | Verificada (2026-09-03) | Revisado por la madre: checklist de 10/11/12 aplicado. Verificación independiente: `npm ci` desde el lockfile sin vulnerabilidades, `npm run typecheck`/`lint`/`test`/`build` sin errores (4/4 pruebas), y además levanté el backend real contra PostgreSQL en Docker y el servidor de Vite apuntándole, y ejercité el flujo completo a través del proxy con `curl` (no solo `fetch` mockeado): login con contraseña incorrecta (401 con el mensaje genérico), login correcto (200 + cookies `session_id` httpOnly y `csrf_token` legible), `GET /auth/me` con sesión, `POST /auth/logout` rechazado sin `X-CSRF-Token` (403) y aceptado con el header (204), y `GET /auth/me` después del logout (401) — todo correcto. Encontré y corregí una cosa antes de aprobar: `index.html` tenía `<html lang="en">` del boilerplate de Vite sin actualizar, pese a que toda la interfaz es en español; lo cambié a `lang="es"` (afecta accesibilidad: lectores de pantalla y corrección ortográfica del navegador usan ese atributo). Re-verifiqué typecheck/test/build después del cambio, sin regresiones. Reviso también los prefijos de ruta que proxya `vite.config.ts` (`/auth`, `/accounts`, `/search`, `/categories`, `/units`, `/attributes`, `/products`, `/variants`, `/imports`, `/health`) contra los routers reales del backend: coinciden exactamente, ninguno de más ni de menos. Coincido con las tres ambigüedades resueltas por la tarea (proxy con lista explícita de prefijos en vez de proxyar todo; `user_name` sin mapear a camelCase; `/` con bienvenida mínima hasta T-011). Sin otras correcciones. Mergeada a main (commits 112a8d4 y 82a7238). Mensaje de commit original de la tarea, sin cambios: `feat(T-010): add SPA skeleton with login and protected routing` / cuerpo: `Implements RF-024, RF-025, RF-039, RNF-007, RNF-012, RNF-015, CU-03, HU-06 (T-010).`. Proyecto nuevo `frontend/` (React 19 + Vite 8 + TypeScript, `npm create vite@latest -- --template react-ts`), estructura según doc12 (`src/api`, `src/features`, `src/shared`). `frontend/vite.config.ts`: proxy de desarrollo hacia el backend (por defecto `http://localhost:8000`, configurable con `VITE_API_PROXY_TARGET`) para los prefijos de ruta que expone la API (`/auth`, `/accounts`, `/search`, `/categories`, `/units`, `/attributes`, `/products`, `/variants`, `/imports`, `/health`), para que el navegador vea todo bajo el mismo origen (doc09) sin tocar el backend. `frontend/src/api/client.ts`: `apiFetch` central que agrega `X-CSRF-Token` desde la cookie `csrf_token` en pedidos que modifican datos, y ante un 401 de cualquier llamada invoca un `unauthorizedHandler` registrable (sesión vencida → RNF-015). `frontend/src/api/auth.ts`: `login`, `logout`, `fetchCurrentAccount` contra `/auth/login`, `/auth/logout`, `/auth/me`. `frontend/src/features/access/`: `AuthContext`/`AuthProvider` (cuenta activa y estado de sesión, registra el `unauthorizedHandler` para volver al login ante un 401 en cualquier momento), `ProtectedRoute` (redirige a `/login` sin sesión válida, muestra un estado de carga mientras se confirma `/auth/me`), `LoginPage` (usuario/contraseña, redirige a `/` si ya hay sesión, muestra error sin distinguir usuario/contraseña inválidos por CU-03 A1). `frontend/src/shared/layout/AppLayout.tsx`: encabezado con el nombre de la cuenta activa y botón "Cerrar sesión", área de contenido (`Outlet`). `frontend/src/shared/HomePage.tsx`: placeholder mínimo de bienvenida en `/` (las pantallas de catálogo, precios, búsqueda e importación quedan fuera de alcance, T-011 a T-015). 4 pruebas de componentes en `frontend/src/App.test.tsx` (Vitest + Testing Library, mockeando `fetch`): redirección al login sin sesión, login exitoso muestra el layout con la cuenta activa, credenciales inválidas muestran el mensaje de error sin navegar, cerrar sesión vuelve al login. `npm run typecheck`, `npm run lint` (Oxlint, sin errores; una advertencia no bloqueante de `react/only-export-components` en `AuthContext.tsx` por exportar `useAuth` junto con el proveedor, patrón habitual de contexto+hook) y `npm run build` sin observaciones. Verificado además en un navegador real (Playwright headless) contra el backend real (`alembic upgrade head` sobre una base PostgreSQL descartable en Docker) sirviendo con `npm run dev`: los cuatro flujos de la lista anterior confirmados visualmente con capturas, sin errores de consola más allá de los 401 esperados de red. Ambigüedades resueltas, para que la madre las revise: (1) el proxy de Vite enumera explícitamente los prefijos de ruta de cada router del backend en vez de proxyar todo por defecto, para no interceptar accidentalmente una ruta futura de la SPA que coincida con un prefijo de la API; es una lista a mantener si un módulo nuevo del backend agrega un prefijo de nivel superior distinto. (2) El campo `user_name` de `AccountResponse` se mantiene tal cual llega de la API (snake_case) en el tipo `Account` del cliente, sin mapear a camelCase, para no agregar una capa de traducción a un objeto tan chico. (3) `/` con sesión válida muestra un mensaje de bienvenida mínimo sin contenido real, porque ninguna pantalla de catálogo entra en esta tarea; T-011 reemplazará o extenderá ese contenido. Sin pendientes no bloqueantes detectados. |
| T-011 | SPA: pantallas de consulta y búsqueda | HU-01, HU-02, HU-03 | task/T-011-search | Verificada (2026-09-04) | Revisado por la madre: checklist de 10/11/12 aplicado, `typecheck`/`lint`/`build` sin observaciones, 11/11 pruebas de frontend corridas de forma independiente. Verificación en vivo propia (backend + frontend reales contra PostgreSQL en Docker, a través del proxy): búsqueda por texto y filtro por categoría devuelven exactamente lo esperado; no repetí la verificación manual en navegador que la propia tarea ya hizo con Playwright (login con marca, búsqueda, sin resultados, error de red con reintento, modo oscuro, viewport móvil) por ser redundante. Coincido con las cuatro decisiones de UX que la tarea dejó para mi revisión: sin carga automática del catálogo completo al entrar (evita traer todo sin que se pida), categorías como chips que togglean, debounce de 300ms, y atributos+label combinados en una línea. El retoque de marca (paleta completa de docs/13, componente `Brand`, corrección de dos colores de error hardcodeados de T-010/T-016 a `var(--danger)`) es prolijo y no toca lo que no debía (`AuthContext`, el selector de negocio de T-016). Sin correcciones. Mergeada a main (commit 6811f48). Mensaje de commit sugerido: `feat(T-011): add search and category browsing screens with Casa Diaco branding` / cuerpo: `Implements RF-002 to RF-009, RF-035, CU-01, CU-02, HU-01, HU-02, HU-03 (T-011); applies the Casa Diaco palette from docs/13-identidad-visual.md and the brand isotype/name to AppLayout and LoginPage as a small T-010 retouch.`. Resumen: reemplacé el placeholder de `/` (`frontend/src/shared/HomePage.tsx`, eliminado) por `frontend/src/features/search/SearchPage.tsx`: campo de búsqueda de texto libre con debounce de 300ms, categorías activas (`GET /categories`, filtradas a `status === "active"` en el cliente) como botones tipo chip que togglean y se combinan con el texto (HU-02, tercer criterio), resultados en tarjetas responsive (`grid-template-columns: repeat(auto-fill, minmax(260px, 1fr))`) con nombre de producto, categoría, variante/atributos (`attribute_name: value`) y precio destacado (`font-size: 20px; font-weight: 600`, formateado en ARS con `Intl.NumberFormat('es-AR', {style: 'currency', currency: 'ARS'})` sobre el string decimal que ya entrega el backend). Sin coincidencias: mensaje claro sin romper la pantalla (HU-01). Falla de red o del servidor: mensaje explícito con botón "Reintentar" en vez de pantalla en blanco (HU-03, último criterio), distinguiendo el mensaje según sea `ApiError` (HTTP) o falla de red (fetch rechazado). Nuevos `frontend/src/api/search.ts` (`fetchCategories`, `searchVariants`) y tipos en `frontend/src/api/types.ts` (`Category`, `AttributeValueSummary`, `SearchResultItem`, `SearchResponse`), reusando `apiFetch` sin tocar `AuthContext` ni el layout existente. Retoque de marca (T-010): paleta completa de docs/13 en `frontend/src/index.css` (`--accent` actualizado, más `--accent-contrast`, `--surface-brand`, `--success`, `--danger`, `--warning`, `--info`, con variantes de modo oscuro); componente nuevo `frontend/src/shared/Brand.tsx` (isotipo "CD" en círculo rojo + nombre, con bajada opcional) usado en `AppLayout` (junto al nombre de cuenta activa, sin tocar el selector de negocio de T-016) y en `LoginPage` (con "Desde 1982", solo ahí). Aproveché el mismo cambio para corregir dos colores de error hardcodeados preexistentes de T-010/T-016 (`#c0392b` en `LoginPage.module.css` y `BusinessSwitcher.module.css`) a `var(--danger)`, ya que docs/13 lo exige explícitamente y la variable no existía todavía cuando se escribieron esos archivos. Actualicé `frontend/src/App.test.tsx`: `SearchPage` ahora dispara `GET /categories` al montar, así que agregué un mock adicional en las dos pruebas que llegan a `/` (login exitoso y cierre de sesión) para no correr el orden de mocks que ya usaban esas pruebas de T-010. 4 pruebas nuevas en `frontend/src/features/search/SearchPage.test.tsx` (búsqueda con resultados con datos suficientes para distinguirlos, sin resultados, filtro por categoría, error de red con mensaje explícito). `npm run typecheck`, `npm run lint` (sin observaciones nuevas; queda el mismo warning preexistente de T-010 en `AuthContext.tsx`) y `npm run test` (11/11, 7 previas + 4 nuevas) y `npm run build` sin errores. Verificación manual además de las automáticas: levanté un PostgreSQL efímero en Docker, corrí las migraciones (que siembran ambos negocios de Casa Diaco, D-039), cargué catálogo de ejemplo vía la API real (categoría "Cintas", dos variantes de "Cinta bebé" por color con el mismo precio para confirmar que el resultado deja claro a cuál corresponde cada importe, y "Aguja de tejer" con precio distinto) y navegué la SPA real con Playwright headless: login con marca e isotipo, pantalla vacía inicial, búsqueda con resultados, filtro por categoría, sin coincidencias, error de red simulado (bloqueando la petición) mostrando el mensaje explícito y el botón "Reintentar", modo oscuro, y viewport móvil (390px, tarjetas en una columna). Sin errores de consola más allá de los 401 esperados antes de iniciar sesión. Decisiones de UX sin especificación explícita, para que la madre las revise: (1) debounce de 300ms al tipear en vez de buscar al confirmar, para no disparar un pedido por tecla, sin debounce al vaciar texto y categoría simultáneamente (transición inmediata a estado inicial sin pedido); (2) sin carga automática de "todo el catálogo" al entrar a la pantalla (el backend lo permite con `q` y `category_id` vacíos): el estado inicial pide escribir un término o elegir una categoría, para no traer todo el catálogo sin que la persona lo pida; (3) categorías como botones tipo chip que togglean (clic de nuevo deselecciona) en vez de un `<select>`, más visual y acorde a "abrir el listado de categorías" de CU-02; (4) atributos y `label` de la variante se combinan en una sola línea separados por "·" cuando ambos existen. Sin pendientes no bloqueantes detectados dentro del alcance de la tarea; no toqué pantallas de alta/edición de catálogo, precios, desactivación/reactivación ni importación (T-012 a T-015), ni contenido de dashboard/inventario/ventas/finanzas (fuera del MVP, D-009). |
| T-012 | SPA: pantallas de mantenimiento del catálogo (productos, variantes, categorías, unidades, atributos), navegación general en sidebar, y adopción de Tailwind CSS (D-041) recreando una referencia visual de Figma | HU-04, HU-09 | task/T-012-catalog-screens | Verificada (2026-09-10) | Mensaje de commit sugerido originalmente (cuerpo desactualizado, ver corrección más abajo): `feat(T-012): adopt Tailwind CSS and restyle catalog and layout screens` / cuerpo: `Implements RF-010 to RF-017, RF-032 to RF-037, RF-040, CU-04, CU-05, CU-08, HU-04, HU-09 (T-012); adds Tailwind CSS (D-041) with the docs/13 palette as the single source of truth, replaces all CSS Modules touched by this task with utility classes, and reworks the catalog and search screens to match the provided Figma reference (sortable/filterable product table, three-dot row menu, product detail as a modal) while dropping what's out of scope (dashboard/inventory/sales/finance, the header live-search, recharts).` **Corrección**: este cuerpo afirma cobertura completa de RF-032 a RF-037; en realidad RF-033 (desactivar/reactivar un valor de atributo) no está implementado porque el backend nunca expuso ese endpoint (ver T-020). El commit realmente hecho y pusheado a `origin/task/T-012-catalog-screens` (por decisión del Responsable, ya que la tarea nunca se había commiteado) usó un mensaje corto sin cuerpo: `feat(T-012): close audit findings before madre review` (commit `2b8c735`), que no repite esa afirmación; no se reescribió ese commit ya pusheado para no forzar un historial reescrito. Resumen (tercera ronda sobre la misma rama; las dos anteriores ya habían dejado las pantallas de catálogo con CSS Modules y el sidebar con nav horizontal-luego-vertical): 1) **Tailwind**: agregué `tailwindcss`/`@tailwindcss/vite` (`frontend/package.json`, `vite.config.ts`) y reescribí `frontend/src/index.css` con `@theme` definiendo una sola vez los tokens de docs/13 (`brand`, `brand-contrast`, `surface-brand`, `success`, `danger`, `warning`, `info`, más `line`/`surface`/`ink` para lo que ya existía como borde/fondo/texto desde T-010) — **corrección (2026-09-05): esta frase quedó desactualizada.** En el momento de escribirla el modo oscuro seguía funcionando vía `prefers-color-scheme: dark`, pero una ronda posterior de esta misma tarea lo eliminó por completo (a pedido del Responsable, ver D-042); no hay ninguna variante oscura vigente. Ningún hex nuevo: son los mismos valores que ya tenía `index.css`, ahora consumidos como `bg-brand`/`text-danger`/etc. en vez de `var(--accent)` sueltas. 2) **Migré a Tailwind y borré el `.module.css`** de todo lo que toca esta tarea: `LoginPage`, `AppLayout`, `Sidebar` (nuevo `AccountMenu.tsx` para el menú de cuenta con "Cerrar sesión", que reemplaza el botón siempre visible), `SearchPage`, y además `Brand` y `BusinessSwitcher` (no pedidos explícitamente, pero usados dentro de Login/sidebar/header y hubiera quedado un sistema de estilos mixto si no se tocaban). 3) **Sidebar** (`frontend/src/shared/layout/navItems.ts` + `Sidebar.tsx`, ya con la lista de configuración de la ronda anterior): fondo `bg-ink text-surface` (invierte la pareja ink/surface a propósito para lograr un panel oscuro sin introducir un color fuera de paleta — en modo claro es un panel oscuro como pide la referencia, en modo oscuro se invierte a un panel más claro que igual contrasta con el contenido), ítem activo `bg-brand`; colapsa a menú hamburguesa hasta 767px (`md:` de Tailwind = 768px, mismo corte de docs/13). Sin Dashboard/Inventario/Ventas/Finanzas/Administración de la referencia (fuera del MVP, D-009); sin buscador general en el header (la búsqueda ya tiene pantalla propia en `/`, T-011). 4) **Pantallas de catálogo**, terminadas y con el estilo de la referencia: `ProductsPage` pasó de tarjetas a una tabla con filtros (texto, categoría, estado) y ordenamiento por columna (nombre/categoría/unidad, sin columna de precio: el precio no forma parte de `Product`/`Variant`, es de T-013); `RowMenu.tsx` nuevo (menú de tres puntos, `items: RowMenuItem[]` como única superficie a tocar para que T-013 agregue "Cambiar precio" y T-014 "Desactivar"/"Activar" sin rehacerlo) con "Ver detalle" y, si `canManageCatalog`, "Editar producto"; `ProductDetailPage` reescrita como una superposición modal (`fixed inset-0`, backdrop, tarjeta centrada con breadcrumb "Catálogo › nombre") en vez de una pantalla de ancho completo, alcanzable con ruta real (`/products/:productId`, cierra navegando a `/products`) y que además abre el formulario de edición automáticamente con `?edit=1` (lo que dispara "Editar producto" desde el menú de fila) — a diferencia de la referencia, que cambia de "sección" con estado de React sin cambiar la URL, acá el modal es una ruta real de React Router (deep-linkeable, con back del navegador funcionando), aunque como consecuencia un enlace directo a `/products/:id` no muestra la tabla de fondo (no está montada). `CategoriesPage`/`UnitsPage`/`AttributesPage` pasaron a tablas con la franja de encabezado clara y "+ Nueva…" como en las pestañas de administración de la referencia. Nada de esto usa `recharts` ni datos de ejemplo: todo sale de `frontend/src/api/catalog.ts` (sin cambios de contrato respecto de las rondas anteriores) contra la API real. El resto de la lógica funcional (alta de producto sin mencionar "variante" si no tiene ninguna, precio inicial obligatorio antes de terminar el alta o el alta de una variante nueva por RN-028, advertencia de `possible_duplicates` después de crear, no antes, gating por rol con `canManageCatalog`) es la misma de las rondas previas, sin cambios de comportamiento. El año del logo se mantuvo en 1982 (el "1980" de la exportación de Figma es un error de esa referencia). 5) **Pruebas**: adapté `App.test.tsx` (el botón "Cerrar sesión" ahora vive dentro del menú de cuenta: los dos tests que lo usaban ahora abren el menú primero con `getByRole('button', {name: /Ada Lovelace/})`) y `CategoriesPage.test.tsx` (el formulario de alta ahora se abre con "+ Nueva categoría" antes de tipear). Reescribí `Sidebar.test.tsx`: con el drawer mobile abierto conviven en el DOM el riel de escritorio y el overlay (jsdom no evalúa los breakpoints de Tailwind), así que la prueba de navegación usa `getAllByRole` y clickea la última coincidencia; las pruebas de ítem activo usan `isOpen=false` para evitar la duplicación. Agregué `ProductsPage.test.tsx` (5 pruebas: listado con categoría/unidad, filtro por texto, filtro por categoría, gating de "Nuevo producto"/"Editar producto" para Empleado, navegación a detalle desde el menú de fila) y `ProductDetailPage.test.tsx` (2 pruebas: cierre navega a `/products`, `?edit=1` abre el formulario de edición automáticamente). No toqué `ProductFormPage.test.tsx` (sigue pasando tal cual: las consultas son por label/rol, no por clases). `npm run typecheck`, `npm run lint` (mismas advertencias preexistentes de patrón `set-state-in-effect` y el warning ya anotado de `AuthContext.tsx`, sin errores nuevos), `npm run test` (27/27: 20 previas + 7 nuevas) y `npm run build` sin errores (CSS final ~20KB sin comprimir); no verifiqué manualmente en navegador contra el backend real. Decisiones de UX sin especificación explícita, para que la madre las revise, además de las tres ya anotadas en la ronda anterior (precio inicial obligatorio, edición/alta de variante en la misma pantalla, atributo nuevo como formulario simple separado): (1) sidebar con fondo invertido ink/surface en vez de un color fijo "siempre oscuro" como la referencia, para no introducir un hex fuera de docs/13 (ver punto 3); (2) corte de sidebar/hamburguesa en 768px (`md:`) en vez de 1024px (`lg:`) como la referencia, siguiendo el pedido explícito de usar el corte de "teléfono" de docs/13 (767px); (3) `ProductDetailPage` como ruta real con apariencia de modal en vez de un modal disparado por estado, por el pedido explícito de mantener el ruteo real; (4) sin paginación en la tabla de productos (la referencia la tiene): no la pidió esta tarea y el catálogo actual es chico; si hace falta con datos reales, es un cambio acotado a `ProductsPage`. Pendiente, no bloqueante: sin pruebas de componente para `UnitsPage`/`AttributesPage` (mismo patrón ya cubierto por `CategoriesPage`, typecheck/lint sí las cubren); no verifiqué manualmente en navegador (paleta en modo oscuro real, comportamiento táctil del hamburguesa, ni el aspecto final contra la referencia de Figma). 6) **Ronda adicional**, tras un mockup general de toda la app (fases 1 a 4) que el Responsable pasó a mitad de tarea: no lo apliqué tal cual (trae Dashboard/Inventario/Ventas/Finanzas/Administración de usuarios/colores hex con código de producto — todo fuera de alcance de T-012 y de esta fase, D-009), pero recorrí las pantallas de catálogo que sí corresponden a esta tarea contra ese documento y encontré violaciones reales de docs/13 (no del mockup nuevo) que corregí: varios botones e inputs habían quedado con `h-9`/`h-8`/`py-1.5` (32-36px) en vez del mínimo de 44px que exige docs/13 ("Alto mínimo 44px" para botones, cualquier control táctil) — afectaba el menú de tres puntos (`RowMenu`), el botón de cerrar del modal de detalle, el hamburguesa del header, el menú de cuenta, el selector de negocio, y los botones "Editar"/inputs de `CategoriesPage`/`UnitsPage`/`AttributesPage`/`VariantAttributesEditor`; de paso corregí el tamaño de fuente de esos mismos inputs de `text-sm` (14px) a `text-base` (16px), que docs/13 pide explícitamente para evitar el zoom automático de iOS. Del mockup general también tomé una idea menor que sí encaja en el alcance ya definido: agregué una columna "Productos" (cantidad) a `CategoriesPage`/`UnitsPage`, con datos que ya se consultan (`GET /products`), sin endpoints nuevos. Reescribí las 4 pruebas de `CategoriesPage.test.tsx` para mockear también `GET /products` (necesario para la columna nueva) y verificar la cuenta por categoría. `npm run typecheck`, `npm run lint` (mismas advertencias preexistentes) y `npm run test` (27/27) y `npm run build` sin errores. 7) **Ronda de auditoría y correcciones** (revisión propia antes de mandar a la madre, sobre la misma rama sin commitear): el Responsable pidió una auditoría de todo lo tocado por esta tarea buscando errores, código sin usar, valores hardcodeados y partes que no siguieran los patrones del resto del proyecto. Encontré y corregí: (1) `ProductFormPage` no verificaba `canManageCatalog` en la propia página (solo se ocultaba el link que lleva a ella), así que una cuenta Empleado podía llegar al alta de producto tipeando la URL — agregué el mismo guard con redirección a `/products` que ya usan el resto de las pantallas de catálogo. (2) Sidebar sin entrada para Categorías/Unidades/Atributos pese a tener pantalla propia y ruta funcionando desde antes — agregué las tres a `navItems.ts` con iconos nuevos en `NavIcon.tsx`. (3) A pedido del Responsable, si al cargar un producto no existe la categoría/unidad/atributo que hace falta, ahora se puede crear ahí mismo sin salir del formulario: agregué "+ Crear categoría nueva…"/"+ Crear unidad nueva…" como opción del propio desplegable en `ProductFormPage` (reusan `createCategory`/`createUnit` ya existentes) y "+ Crear atributo nuevo…" en `VariantAttributesEditor` (reusa `createAttribute`; al crear un atributo nuevo abre directo el campo de "nuevo valor" porque todavía no tiene ninguno), compartido por `ProductFormPage` y `ProductDetailPage`. (4) `ProductDetailPage.load()` no tenía guarda de concurrencia: si la usuaria navegaba a otro producto antes de que terminara de cargar el anterior, la respuesta tardía podía pisar el estado del producto ya en pantalla — agregué un id de pedido (`requestIdRef`) que descarta cualquier respuesta que ya no sea la última. (5) Editar un producto desde el modal de detalle no se reflejaba en la tabla de `ProductsPage` hasta recargar la página entera — `ProductsPage` ahora pasa un callback `onProductUpdated` a su `<Outlet>` (React Router `useOutletContext`) que `ProductDetailPage` invoca después de guardar, sin pedir la lista de nuevo. (6) `handleSavePrices` (precio inicial al crear un producto con variantes) reintentaba variantes que ya se habían guardado bien en un intento anterior, con `expected_current_price_id: null`, chocando contra el precio que esa variante ya tenía — ahora lleva un set de variantes ya guardadas y las saltea en el reintento. (7) El menú de tres puntos (`RowMenu`) calculaba su posición una sola vez al abrirse y quedaba flotando en coordenadas viejas si se scrolleaba la tabla; ahora se cierra solo ante scroll o resize (no se tocó que el panel pueda sobresalir de la fila: es intencional, ya documentado en el propio componente). (8) El modal de bienvenida (`WelcomeModal`) cubría el header con un fondo que capturaba clicks reales durante sus primeros 5 segundos, de forma que el primer click sobre el menú de cuenta lo cerraba en vez de abrir el menú — el fondo ahora es `pointer-events-none` y solo la tarjeta central sigue siendo clickeable. (9) `SelectMenu` (reemplazo del `<select>` nativo en todos los filtros/formularios) no tenía navegación por teclado — agregué flechas arriba/abajo, Home/End y Escape, y de paso quité un `containerRef` que quedó sin uso. (10) La acción "Cambiar precio" de la tabla de productos era un botón deshabilitado permanente pese a que `ChangePriceModal` ya funciona en el detalle de producto — ahora navega a `/products/:id?changePrice=1`, que abre el modal automáticamente si el producto tiene una sola variante (si tiene varias, abre igual el detalle para elegir cuál, sin agregar ahí la lógica completa de precios por producto que le corresponde a T-013). (11) Quité comentarios que violaban "sin comentarios en el código" (docs/12) en `index.css`, `RowMenu.tsx` y `ProductDetailPage.tsx`. Decisión del Responsable a documentar, no un bug: el Panel (`DashboardPage`) se mantiene como pantalla de inicio; la búsqueda de productos vive en la tabla de `/products` (texto, categoría y estado) en vez de una pantalla de búsqueda dedicada — la pantalla de T-011 (`SearchPage`) sigue borrada a propósito. Ojo: como la tabla de productos todavía no puede mostrar el precio vigente (columna "Precio" en "Próximamente", bloqueada por T-017), la consulta de precio de HU-01 no queda cubierta hasta que se resuelva esa tarea de backend. 4 pruebas nuevas (`ProductFormPage.test.tsx`: guard de permisos, creación inline de categoría/unidad, creación inline de atributo; `ProductsPage.test.tsx`: edición reflejada en la tabla sin recargar) corridas junto con las previas. `npm run typecheck`, `npm run lint` (mismas advertencias preexistentes, ninguna nueva) y `npm run test` (32/32) y `npm run build` sin errores. **Revisión independiente de la madre (2026-09-05): Necesita ajustes, no mergear todavía.** Esta es la primera revisión genuinamente independiente de esta rama (las rondas anteriores, incluida la de auditoría, las hizo la misma conversación que implementó la tarea). Confirmé que el diff contra `main` no toca nada de `backend/`: todo el trabajo de esta rama (incluida la ronda de auditoría) quedó en un único commit (`2b8c735`) sobre `main` en `6811f48` (que ya incluye T-011). Corrí de forma independiente `npm install`, `npm run typecheck`, `npm run lint`, `npm run test` (32/32 ✓) y `npm run build`: los cuatro limpios, coinciden con lo reportado. No pude validar `pytest` del backend en mi entorno (falla generalizada de sesión/cookie en ~102 pruebas, incluidas migraciones y tareas ya verificadas, por lo que parece una incompatibilidad de versiones FastAPI/Starlette/httpx del `TestClient` en mi entorno) — como esta rama no toca ningún archivo de `backend/` no es una regresión de T-012 y no cambia el veredicto, pero lo señalo aparte para que se revise. Contrastando el código contra los RF/CU/HU que esta tarea dice cubrir (no solo los nombres de función), encontré: (1) **Regresión de HU-01/HU-03** (ya `Verificada` en T-011, 2026-09-04): esta rama borra `SearchPage`/`formatPrice`/su test y CSS, y la ruta `/` pasa a ser un `DashboardPage` placeholder ("En construcción"). La búsqueda rápida con precio visible desaparece: `ProductsPage` filtra por texto/categoría/estado, pero su columna "Precio" es un "Próximamente" fijo (bloqueado por T-018), y no queda ninguna pantalla donde ver varios precios de un vistazo. El resumen de esta fila dice que es "Decisión del Responsable a documentar, no un bug", pero no encontré ningún D-0XX en `docs/08-trazabilidad-y-decisiones.md` que registre esa decisión, a diferencia de cualquier otro cambio de alcance de este proyecto. Pido que el Responsable confirme explícitamente que autorizó perder HU-01/HU-03 hasta que se resuelva T-018, y que quede asentado como decisión en docs/08, no solo en esta celda. (2) **RF-016/CU-10 sin confirmación al desactivar o reactivar un producto**: `ProductsPage.tsx` (`toggleActive`) y `RowMenu.tsx` ejecutan `deactivateProduct`/`reactivateProduct` de inmediato al tocar el ítem del menú, sin ningún diálogo intermedio. CU-10 exige explícitamente que "el sistema explica el efecto y solicita confirmación" antes de aplicar el cambio. No es una limitación técnica: la misma tarea sí implementó ese patrón para el cambio de precio (`ChangePriceModal`, con Confirmar/Cancelar); quedó sin hacer para desactivar/reactivar producto (la fila de T-014 de abajo ya deja anotado, correctamente, que lo que falta a nivel variante es tarea de T-014 — esto es sobre el nivel producto que T-012 sí agregó). (3) **Modo oscuro eliminado sin avisar**: en `main`, `index.css` tenía un bloque `@media (prefers-color-scheme: dark)` completo con su propia paleta oscura; la migración a Tailwind de esta rama lo borra entero y fija `color-scheme: light` a secas en `:root`. Esto contradice al propio `docs/13-identidad-visual.md` que esta misma rama actualizó (pide explícitamente "Configurar el modo oscuro de Tailwind acorde... para no perder ese soporte al migrar") y al mensaje de commit sugerido de esta fila, que asegura que "las mismas variables se redefinen bajo `prefers-color-scheme: dark`... la cascada alcanza sin `dark:`". No encontré ninguna definición de modo oscuro en `frontend/src` (sin resultados para `prefers-color-scheme` ni `dark:`). (4) **Archivo fuera de alcance en el repositorio**: `frontend/shot.cjs` (nuevo) es un script de depuración con una ruta absoluta hardcodeada al directorio temporal de otra sesión de IA (`C:/Users/ASPIRE~1/AppData/Local/Temp/claude/.../scratchpad/login.png`) y depende de `playwright`, que no está declarado en `package.json`. No puede ejecutarse tal cual y no tiene relación con el alcance de la tarea; hay que borrarlo antes de mergear. Además, menos urgentes pero a corregir o documentar: (5) **HU-09/RF-033 sigue sin poder cumplirse en ningún lado**: no existe endpoint de backend para desactivar un valor de atributo (`update_attribute_value` solo cambia el texto), así que "Desactivar un color impide usarlo en variantes nuevas" (criterio explícito de HU-09) no se puede implementar todavía ni en el frontend ni en el backend. El mensaje de commit sugerido de esta fila afirma cubrir RF-032 a RF-037 y HU-09 completos; eso hay que corregirlo (la cobertura es parcial) y conviene anotar el endpoint que falta como trabajo de backend pendiente. (6) El punto (11) de la ronda de auditoría ("Quité comentarios que violaban sin comentarios en el código... en `index.css`, `RowMenu.tsx` y `ProductDetailPage.tsx`") no es exacto: sigue un comentario de tres líneas en `RowMenu.tsx` (por qué el menú se cierra al hacer scroll/resize) y otro de dos líneas en `frontend/src/shared/layout/navItems.ts` (por qué algunos ítems quedan `disabled`). Ninguno es grave por sí solo, pero es un buen ejemplo de por qué hacía falta esta revisión independiente en vez de confiar en la autorrevisión. (7) **Tamaños de texto inconsistentes con la propia pauta de accesibilidad del proyecto**: `docs/13` fija 16px como base de cuerpo, y el proyecto prioriza texto y controles grandes por defecto para usuarias de más de 50 años. `ProductsPage`/`ProductFormPage` (rehechas contra la referencia de Figma) usan `text-lg`/`text-base` de forma consistente, pero `CategoriesPage.tsx`, `UnitsPage.tsx`, `AttributesPage.tsx` y buena parte de `ProductDetailPage.tsx` quedaron en `text-sm` (14px) para texto de tabla y etiquetas, e incluso `text-xs` (12px) en los botones de fila "Editar"/"Ver valores" — por debajo de la base del propio proyecto y en contraste con el resto de las pantallas de esta misma tarea. (8) Menor, no bloqueante: `VariantAttributesEditor.tsx` sigue usando `<select>` nativo en vez del `SelectMenu` compartido que usan el resto de los formularios de la misma tarea (`ProductFormPage`, `ProductDetailPage`); no rompe nada (esos `<select>` ya cumplen 44px/16px), pero es una inconsistencia del sistema de diseño que esta tarea se propuso unificar. Lo que verifiqué que sí está bien: el diff se limita a `frontend/` y `docs/` (nada de `backend/`); D-041 (adopción de Tailwind) está debidamente registrada en docs/08 con su justificación; el gating por rol (`canManageCatalog`) es consistente en las seis pantallas nuevas vía un único helper (`roles.ts`), sin duplicación; todas las rutas de catálogo quedan detrás de `ProtectedRoute` (RNF-012); no hay contraseñas, claves ni datos reales del negocio en el diff; la falta de deactivate/reactivate de variante NO es un defecto de esta tarea (ver nota de la fila T-014, correctamente diferida); `typecheck`/`lint`/`test` (32/32)/`build` pasan limpios de forma independiente, igual que lo reportado. Con los puntos 1 a 4 resueltos (y una respuesta del Responsable sobre el punto 1) volvé a dejarla en "Lista para revisión" para una nueva pasada. **Respuesta de la conversación de tarea a los ajustes pedidos (2026-09-05):** (1) El Responsable confirmó explícitamente que autoriza reemplazar la búsqueda dedicada por los filtros de `/products`; quedó asentado como D-043 en docs/08 (con la salvedad de que el precio en esa tabla sigue bloqueado por **T-017**, no T-018 como decía esta revisión — T-018 es paginación, T-017 es el que agrega precio y código a `GET /products`). (2) Agregué `frontend/src/shared/ConfirmDialog.tsx` (mismo patrón visual que `ChangePriceModal`/`WelcomeModal`) y lo conecté en `ProductsPage.tsx`: "Desactivar"/"Activar" del menú de fila ahora abre el diálogo (explica el efecto, CU-10 paso 3) y solo llama a `deactivateProduct`/`reactivateProduct` al confirmar; actualicé la prueba existente y agregué una nueva que confirma que cancelar no dispara ningún pedido. (3) El Responsable confirmó que no quiere modo oscuro en esta etapa; quedó asentado como D-042 en docs/08, actualicé `docs/13-identidad-visual.md` (tabla de colores a un solo valor por token, sin columna "Oscuro", saqué la frase que pedía configurar `darkMode` de Tailwind) y corregí la frase desactualizada de esta misma celda (ver arriba). (4) Borré `frontend/shot.cjs`. (5) Agregué **T-020** al registro (backend: exponer desactivar/reactivar un valor de atributo) y corregí la afirmación de cobertura completa en el campo de mensaje de commit de esta fila (ver arriba). (6) Borré los dos comentarios que quedaban (`RowMenu.tsx`, `navItems.ts`). (7) `CategoriesPage.tsx`, `UnitsPage.tsx`, `AttributesPage.tsx` y las partes de `ProductDetailPage.tsx` que quedaban en `text-sm`/`text-xs` ahora usan `text-base`/`text-sm` como mínimo, igual que `ProductsPage`/`ProductFormPage`. (8) No toqué el `<select>` nativo de `VariantAttributesEditor.tsx`: la propia revisión lo marcó como no bloqueante, y reemplazarlo por `SelectMenu` obliga a reescribir las pruebas que dependen de `user.selectOptions` (que solo funciona contra un `<select>` real) sin que nada lo exija ahora mismo — queda anotado para una tarea futura si se quiere unificar. Verificación tras estos cambios: `npm run typecheck`, `npm run lint` (mismas advertencias preexistentes, ninguna nueva) y `npm run test` (33/33: 32 previas + 1 nueva sobre cancelar el diálogo de confirmación) y `npm run build`, todos sin errores. **Segunda revisión de la madre, tras los ajustes (2026-09-05): apruebo.** Verifiqué cada punto de forma independiente, no solo el texto de esta celda: leí el diff completo del commit `20ea3a8` contra `2b8c735`; confirmé en docs/08 que D-042 y D-043 están redactadas con la justificación del Responsable; confirmé en `docs/13-identidad-visual.md` que la tabla de colores ya no promete una variante oscura; confirmé que `frontend/shot.cjs` ya no existe; leí `ConfirmDialog.tsx` y su uso en `ProductsPage.tsx` (el diálogo explica el efecto y solo llama a `deactivateProduct`/`reactivateProduct` al confirmar, con prueba de que cancelar no dispara ningún pedido); confirmé que `RowMenu.tsx` y `navItems.ts` ya no tienen los comentarios señalados; confirmé que `CategoriesPage.tsx`, `UnitsPage.tsx`, `AttributesPage.tsx` y `ProductDetailPage.tsx` ya usan `text-base` como mínimo para contenido primario; y confirmé que la fila de T-020 (nueva) documenta correctamente el hueco de backend de RF-033/HU-09, con la corrección de la afirmación de cobertura en el cuerpo del commit de esta misma fila. Corrí de nuevo, de forma independiente, `npm run typecheck`, `npm run lint` (mismas advertencias preexistentes, ninguna nueva), `npm run test` (33/33 ✓) y `npm run build`: los cuatro limpios. Gracias por la corrección del punto (1): T-017 es el que agrega precio a `GET /products`, no T-018 (que es paginación) — mi revisión anterior lo cruzó mal, quedó bien corregido acá. No encontré nada nuevo fuera del alcance ya cubierto por T-020 (RF-033) y T-017 (precio en la tabla). **Lista para commitear y mergear a `main`** con los commits ya existentes en la rama (`2b8c735` y `20ea3a8`, ambos ya hechos por el Responsable) — no hace falta un commit adicional. Aviso al Responsable: marco esta fila `Verificada` con la fecha en cuanto confirmes que hiciste el merge. |
| T-013 | SPA: pantallas de precios (cambio individual, por producto e historial) | HU-05, HU-10 | task/T-013-price-management | Verificada (2026-09-06) | Depende de T-010. Backend ya existía completo (backend/app/api/pricing.py); tarea fue solo frontend. Revisión independiente de la madre (2026-09-06): pantalla /precios reutiliza el estilo de tabla de Productos, cambio individual y masivo por producto (CU-06 A3/A4), manejo de conflicto 409 con re-confirmación (A2), historial por variante (CU-07), modo solo lectura para Empleado, sin comentarios ni colores hardcodeados. Encontré duplicación real: se extrajo `useTableScrollbar.ts` para PricingPage pero ProductsPage seguía con su copia inline; pedí el refactor y se aplicó correctamente conservando las mismas dependencias del efecto. Typecheck, test (57/57) y build limpios en ambas rondas. Mergeable. |
| T-025 | Selector de negocio a pantalla completa tras el login, exclusivo de Administrador (D-043, D-044) | HU-?? | task/T-025-business-selector | Verificada (2026-09-06) | Depende de T-026. Decisión de la madre (2026-09-06): la pantalla se muestra en cada login, sin recordar la elección anterior. Revisión independiente de la madre (2026-09-06): POST /auth/active-business ahora exige rol Administrador en el backend (require_role), con test que confirma 403 para Gerente/Empleado aun con acceso a ambos negocios; el resto del backend (resolución por defecto al de menor id) ya existía. En el frontend, ProtectedRoute muestra BusinessSelectorPage a pantalla completa solo cuando justLoggedIn && Administrador && >1 negocio; se reemplazó el dropdown BusinessSwitcher del header por un selector dentro de AccountMenu (también exclusivo de Administrador) para cambiar de negocio durante la sesión, y se agregó el nombre del negocio activo al header. Sin comentarios ni colores hardcodeados. Typecheck, test frontend (58/58) y build limpios; ruff check/format y test suite completa del backend (131/131) limpios. Mergeable. |
| T-014 | SPA: pantallas de desactivación y reactivación | HU-08 | task/T-014-variant-deactivation | Verificada (2026-09-06) | Depende de T-010 y T-012. Nota (detectada en la auditoría de T-012): la desactivación/reactivación de **producto** ya quedó resuelta como parte de T-012 (`ProductsPage.tsx`, acciones "Desactivar"/"Activar" del menú de fila, contra los endpoints de T-006). Lo que faltaba acá era solo el nivel de **variante**: el backend ya exponía `POST /variants/{id}/deactivate` y `/reactivate` (T-006), pero no existía `deactivateVariant`/`reactivateVariant` en `frontend/src/api/catalog.ts` ni acción alguna en `ProductDetailPage` para invocarlos. Revisión independiente de la madre (2026-09-06): agrega esas dos funciones al cliente API (mismo patrón que las de producto), un botón "Desactivar"/"Activar" por fila de variante con `ConfirmDialog` explicando el efecto antes de aplicar el cambio (CU-10), un rótulo "Inactiva" y ocultamiento de "Ver historial"/"Editar" cuando la variante está inactiva. Confirmé que la variante implícita única de un producto sin variantes reales no puede desactivarse por separado (la sección completa de variantes no se renderiza en ese caso, solo el toggle de estado del producto). Typecheck, test (67/67) y build limpios. Mergeable. |
| T-015 | SPA: pantalla de importación (vista previa y confirmación) | HU-07 | — | Pospuesta (2026-09-06) | El Responsable prefiere cargar su catálogo real ahora mismo directo contra la API de importación ya existente (T-007, `POST /imports`), sin construir una pantalla todavía; se retoma esta tarea si hace falta self-service repetido más adelante. El Responsable prefiere esperar a que T-018 (paginación) esté resuelta antes de pasar el archivo real. Depende de T-010 y de la API de T-007 (ya mergeada). |
| T-016 | Negocio activo por cuenta: alta del segundo negocio de Casa Diaco (mercería y despensa) y resolución del negocio activo según los accesos reales de la cuenta, en vez de "el primer negocio" | RF-039, RN-031, RNF-017, RNF-018, D-017, D-018, D-039 | task/T-016-active-business | Verificada (2026-09-03) | Revisado por la madre: checklist de 10/11/12 aplicado, 117 pruebas de backend (105 previas + 11 de la tarea + 1 mía) y 7 de frontend (4 previas + 3 de la tarea) corridas de forma independiente contra PostgreSQL real, sin drift de `alembic check`, sin observaciones de `ruff`/`oxlint`/`typecheck`/`build`. Además de correr las pruebas automáticas, verifiqué el aislamiento en vivo: levanté el backend y el frontend reales, di de alta un segundo negocio por SQL directo (igual que haría la migración) y confirmé a través del proxy real que la misma variante tiene precios completamente independientes por negocio, que una cuenta de un solo negocio no puede forzar el cambio al otro (403) ni ve su precio, y que el selector de negocio aparece solo con más de un acceso — la propia tarea no había hecho esta verificación manual, solo pruebas automáticas. El diseño central (`list_accessible_businesses`, `resolve_active_business`, `set_active_business`, y `get_active_business` como dependencia FastAPI de la que ahora depende `require_role`) es correcto y prolijo; coincido con la resolución de dejar `catalog.py` usando `Account.organization_id` en vez de la resolución por negocio, porque el catálogo es de la organización (D-017), no del negocio. Encontré y corregí dos cosas antes de aprobar: (1) dos endpoints (`login`, `change_active_business`) repetían como string literal el mensaje de error que ya existía centralizado en `NO_BUSINESS_ACCESS_DETAIL`/`BUSINESS_NOT_ACCESSIBLE_DETAIL` (`constants/access.py`) en vez de reutilizarlo (doc12, "sin valores hardcodeados"); (2) siguiendo un comentario del Responsable durante la revisión: `GET /accounts` devolvía todas las cuentas de la organización sin distinguir negocio, así que una cuenta de mercería aparecía igual al administrar cuentas desde despensa — agregué el filtro por negocio activo a `list_accounts` (ahora recibe `business_id`) y sumé `businesses: list[BusinessSummary]` a `AccountResponse` (no solo a `SessionInfoResponse`) para que una cuenta con acceso a más de un negocio se identifique como tal en cualquier listado, en vez de aparecer indistinguible de una de un solo negocio; agregué una prueba nueva (`test_account_listing_is_scoped_to_the_active_business`) que verifica que una cuenta de mercería no aparece en el listado de despensa y viceversa, y que una cuenta con ambos accesos los muestra. Mensaje de commit sugerido para mis dos ajustes: `fix(T-016): scope account listing to the active business and reuse error constants` / cuerpo: `Accounts are now listed per active business (with each account's businesses shown) and two endpoints reuse the existing NO_BUSINESS_ACCESS_DETAIL/BUSINESS_NOT_ACCESSIBLE_DETAIL constants instead of repeating the string (T-016 madre review).` — a commitear como commit separado del de la tarea. Sin otras correcciones. Mergeada a main (commits f703299, ccc50ba, merge acb54de). Mensaje de commit original de la tarea: `feat(T-016): resolve active business per account instead of a single global business` / cuerpo: `Implements RF-039, RN-031, RNF-017, RNF-018 (D-017, D-018, D-039): a second business can be seeded via INITIAL_BUSINESS_2_NAME/INDUSTRY, AccountSession tracks an explicit active business for accounts with more than one, every endpoint that used to read "the" business now resolves it from the authenticated account's real access, and the SPA shows a business selector only when it has more than one (T-016).`. Backend: migración nueva `7e2b9f4a1c86_add_second_business_and_session_business.py` (agrega `account_session.active_business_id`, FK opcional a `business`, y siembra un segundo negocio solo si `INITIAL_BUSINESS_2_NAME` e `INITIAL_BUSINESS_2_INDUSTRY` están ambas configuradas, sin valor por defecto, otorgándole acceso de Administrador a la cuenta admin inicial; si falta alguna, no crea nada, para no romper una instalación de un solo negocio); downgrade probado hasta `base`. `backend/app/domain/access/active_business.py` reescrito como lógica de dominio pura: `list_accessible_businesses` (negocios activos con `BusinessAccess` activo para la cuenta, ordenados por id), `resolve_active_business` (si la cuenta tiene acceso a uno solo, ese es el activo; si tiene varios, usa el guardado en la sesión si sigue siendo accesible, si no el de menor id, sin escribirlo hasta que la cuenta cambie explícitamente) y `set_active_business` (valida el acceso antes de persistir el cambio en `AccountSession`, RNF-018). `backend/app/domain/access/permissions.py` agrega la dependencia FastAPI `get_active_business` (resuelve por cuenta+sesión autenticada, 403 si la cuenta no tiene acceso a ningún negocio) y `require_role` pasa a depender de ella en vez de leer "el primer negocio". Todo endpoint que antes llamaba a la función vieja (`search`, `pricing`, `imports`, `accounts`) ahora recibe el negocio resuelto por dependencia; `catalog.py` dejó de necesitar la resolución de negocio porque el catálogo es de la organización (docs/07), no del negocio, y `Account.organization_id` ya alcanza. Nuevo endpoint `POST /auth/active-business` (con CSRF) que valida el acceso antes de cambiar; `GET /auth/me` y `POST /auth/login` devuelven además `active_business_id` y `businesses` (id/nombre/rubro) para que la SPA sepa cuándo mostrar el selector. `create_account`/`update_account` (cambio de rol) reciben ahora el negocio activo de quien actúa, para decidir a qué negocio queda vinculada la cuenta nueva o en qué negocio se busca su rol (no había un caso de uso multi-negocio para gestión de cuentas en el alcance de esta tarea). Frontend: `frontend/src/features/access/BusinessSwitcher.tsx` (invisible con un solo negocio, `<select>` si hay más de uno, llama a `/auth/active-business` y refresca la cuenta activa), integrado en `AppLayout`; `AuthContext` expone `switchBusiness`; tipo `Account` extendido con `active_business_id`/`businesses`. Pruebas nuevas: `backend/tests/test_active_business.py` (9 pruebas: `/auth/me` y login informan un solo negocio por defecto, cambiar a un negocio sin acceso se rechaza forzando el pedido directamente —no solo confiando en que la interfaz no lo ofrezca—, tanto para un negocio real como para un id inexistente, rechazo sin CSRF, una cuenta con acceso a ambos negocios cambia y cada consulta de precio refleja el negocio activo correcto, y una cuenta con acceso a un solo negocio no ve ni puede cambiarse al precio del otro vía `/search`) y 2 pruebas nuevas en `test_migrations.py` (sin las variables de entorno del segundo negocio no se crea nada; con ambas configuradas crea el negocio y el acceso del Administrador, con downgrade limpio). Ninguna de las pruebas ya existentes se modificó. 116 pruebas de backend corridas contra PostgreSQL real (105 previas + 11 nuevas), todas verdes; `alembic check` sin drift; `ruff check` y `ruff format --check` sin observaciones. Frontend: `npm run typecheck`, `npm run lint` (mismo warning preexistente de `AuthContext.tsx` ya anotado en T-010, no nuevo), `npm run test` (7 pruebas: 4 previas sin cambios de comportamiento más 3 nuevas de `BusinessSwitcher`) y `npm run build`, todos sin errores. No verifiqué manualmente en un navegador real (sí con Vitest/Testing Library y con las 9 pruebas de integración de backend contra PostgreSQL real, incluida la que ejercita el flujo completo de cambio de negocio a través de `/search`). Nada pendiente que yo haya detectado dentro del alcance de la tarea. |
| T-017 | Backend: incluir el precio vigente de cada variante en la respuesta de `GET /products` (listado de catálogo), para que la tabla de productos de la SPA (T-012) pueda mostrar la columna Precio sin pedirlo variante por variante | RF-007, RF-035 | task/T-017-price-in-catalog-listing | Verificada (2026-09-05) | El modelo y los endpoints de precio (`GET/PUT /variants/{id}/price`, `GET /variants/{id}/prices`, `PUT /products/{id}/price`) ya existen desde T-004 (mergeada), igual que `get_current_prices_for_variants` (búsqueda en lote, T-005); falta que el listado de catálogo los reutilice. Bloquea que `ProductsPage` muestre precio y la acción "Cambiar precio" desde ahí; ver también T-013. **Alcance recortado (2026-09-05):** esta fila incluía originalmente agregar un campo de código de producto/variante; se saca de acá porque choca con una decisión ya tomada — docs/01-vision-y-alcance.md pone los códigos de barras explícitamente "Fuera del MVP" y como "Etapa 4: identificación por código" (después de reposición e inventario, con la razón documentada de que su beneficio es acelerar carga de ventas/stock, no la consulta de precio), y D-020 ya fija que el código va en la variante "en una etapa posterior". Adelantarlo ahora contradiría ese orden de etapas decidido a propósito. La columna "Código" de `ProductsPage` (T-012) sigue en "Próximamente" por mucho más tiempo que esta tarea — no es un pendiente de T-017, es un pendiente de la Etapa 4 completa. **Implementación (2026-09-05):** Mensaje de commit sugerido: `feat(T-017): include current price in GET /products response` / cuerpo: `Adds price_amount to each variant in GET /products by reusing get_current_prices_for_variants (T-005) instead of reimplementing the current-price filter. Since the endpoint now needs the active business to know which business's price to return, it starts depending on get_active_business — the catalog itself stays organization-scoped (D-017); only the price lookup is business-scoped, same pattern as /search and pricing.py.` Archivos tocados: `backend/app/api/catalog.py` (agrega `price_amount: Decimal \| None` a `VariantResponse`; `_variant_response`/`_product_response` reciben opcionalmente el mapa de precios vigentes, sin afectar los demás endpoints que ya usan esos helpers; `list_products` ahora depende de `get_active_business` y llama a `get_current_prices_for_variants` con los ids de variante del listado) y `backend/tests/test_catalog.py` (agrega `test_list_products_includes_current_price_per_variant`: una variante con precio vigente y otra sin precio en el mismo producto, verifica `price_amount` como string decimal y `null` respectivamente). No toqué ningún campo de código de producto/variante ni la columna "Código" del frontend, según el recorte de alcance ya registrado en esta fila. Corrí toda la suite de backend (`pytest`): mi test nuevo pasa; hay 7 fallas preexistentes en `test_active_business.py` y `test_migrations.py` que confirmé (con `git stash`) que ya fallan igual sin mis cambios, así que no son una regresión de esta tarea. No toqué el frontend (T-012 ya tiene la columna "Precio" esperando este endpoint). **Revisión independiente de la madre (2026-09-05): Verificada.** Esta tarea la implementó una conversación distinta de la que escribió T-012, así que esta sí es una revisión genuinamente independiente. Leí el diff completo contra `main` (solo `backend/app/api/catalog.py`, su test y esta fila — nada de frontend, como correspondía). Contrasté el código contra RF-007 y RF-035: `list_products` ahora depende de `get_active_business` (mismo patrón que `/search` y `pricing.py`) y reutiliza `get_current_prices_for_variants` (T-005) con una sola consulta `IN` por lote, sin N+1; el campo nuevo es `price_amount: Decimal \| None` (nunca float, RN-023). Apliqué el checklist de seguridad (docs/11): sin contraseñas ni cadenas de conexión en el diff, sin datos reales del negocio en la prueba nueva, y RNF-007/RNF-012 se preservan (`get_current_user` sigue exigido; `get_active_business` es una capa adicional, no un reemplazo, y ya se usa así en el resto del proyecto). Confirmé `ruff check`/`ruff format --check` limpios y `alembic check` sin drift (no hace falta migración, correcto). Corrí `pytest` de forma independiente (contenedor Docker local, no el mismo entorno que usó la tarea): 111 pasan, 7 fallan — los mismos siete que reporta la tarea. Los verifiqué en `main` también (sin los cambios de esta rama) y fallan igual, así que confirmo que no son una regresión de T-017. Investigué la causa real (la tarea no la había diagnosticado, solo confirmado que preexistía): no es una incompatibilidad de versiones de FastAPI/Starlette/httpx como había señalado la revisión anterior de T-012 en un entorno distinto — es que el `backend/.env` compartido de este entorno de desarrollo tiene configuradas `INITIAL_BUSINESS_2_NAME`/`INITIAL_BUSINESS_2_INDUSTRY` (necesarias para probar T-016 a mano), y esas siete pruebas de `test_active_business.py`/`test_migrations.py` asumen un escenario de un solo negocio sin aislar su propio entorno (no hacen `monkeypatch` de las variables ni limpian el `@lru_cache` de `get_settings`), así que heredan la configuración ambiente de dos negocios y fallan. No es un defecto de T-016 ni de T-017; es una fragilidad de esas pruebas específicas que vale la pena que una tarea futura corrija (aislar su propio entorno en vez de depender del `.env` de quien la corre). Un detalle menor, no bloqueante: como `VariantResponse` es un modelo compartido, el campo `price_amount` nuevo también aparece (siempre `null`) en `GET /products/{id}`, que no estaba en el alcance de esta tarea; es inofensivo (nada lo consume ahí) y no vale la pena evitarlo a costa de duplicar el modelo. Mergeable a `main` con el mensaje de commit ya sugerido por la tarea. |
| T-018 | Backend: paginación real de `GET /products` (parámetros `page`/`page_size`, filtro por categoría/estado resuelto en el servidor, y total de resultados en la respuesta), y el consumo correspondiente en `ProductsPage` (SPA) | RF-010 a RF-017 | task/T-018-catalog-pagination | Verificada (2026-09-06) | Hoy `GET /products` devuelve el catálogo completo y `ProductsPage` filtra/ordena todo en el navegador; funciona mientras el catálogo es chico (T-012 lo dejó así a propósito), pero no escala a un catálogo de miles de productos, donde traer todo de una sola vez es mala práctica (ancho de banda, memoria, tiempo de carga). Se evaluó agregar paginación solo del lado del cliente (sin tocar el backend) y se descartó por la misma razón: seguiría bajando el catálogo entero. Diseño de UI ya acordado con el Responsable para cuando el backend la soporte: selector de cantidad por página (10/25/50), botones Anterior/Siguiente con números de página, comprimidos con "…" cuando hay más de 10 páginas, mostrando siempre la 1 y la 2. Alcance ampliado (2026-09-06): el Responsable confirmó que hace falta paginar en más lugares, no solo `GET /products` — por ahora quedan anotados para cuando corresponda: el historial de precios (`GET /variants/{id}/prices`, cuando exista T-013) y los resultados de búsqueda si se retoma una pantalla de búsqueda, sobre todo si un producto llega a tener muchas variantes. No se agrega paginación a Categorías/Unidades/Atributos (el Responsable no lo pidió). Esta tarea (T-018) implementa primero el caso de `GET /products`; los demás lugares se resuelven cuando la pantalla que los necesita exista, reutilizando el mismo mecanismo.<br><br>**Resumen de esta conversación de tarea:** Backend — `products.list_products` ahora acepta `page`/`page_size`/`category_id`/`status`/`search` y devuelve `(items, total)`; cuando `page` y `page_size` se omiten (ninguno de los dos) devuelve el catálogo completo sin paginar, para no romper a `CategoriesPage`/`UnitsPage`, que siguen usando `fetchProducts()` sin parámetros solo para calcular conteos por categoría/unidad (no se les agregó paginación, fuera de alcance). El endpoint `GET /products` valida `page_size` contra {10, 25, 50} y `status` contra los valores válidos, y devuelve `{items, total, page, page_size}`. Frontend — `ProductsPage` ahora pide la página al servidor con `fetchProductsPage()` (nueva función en `api/catalog.ts`) en vez de filtrar/paginar en el cliente; agrega selector de cantidad por página y controles Anterior/Siguiente con números comprimidos (`shared/Pagination.tsx` + `shared/paginationList.ts`, con su propio test unitario de `buildPageList`). El orden por columna se limitó a Nombre únicamente (a pedido del Responsable, Categoría/Unidad dejaron de ser ordenables) y se mantiene como una comodidad puramente visual sobre la página actual (no pide al servidor una página distinta ni afecta la paginación), ya que el backend no expone `order_by`.<br><br>**Ambigüedad resuelta (la señalada explícitamente por el Responsable):** el buscador de texto libre por nombre, que antes filtraba en el cliente, ahora se resuelve en el servidor — se sumó `search` como parámetro de `GET /products` (coincidencia parcial, case-insensitive, sobre `Product.name`). Sin este cambio el buscador solo hubiera encontrado resultados dentro de la página visible. El campo de búsqueda tiene debounce de 300&nbsp;ms antes de pedir al servidor, para no disparar una consulta por cada tecla.<br><br>**Pulido de UI hecho a pedido del Responsable sobre la tabla ya paginada:** encabezado fijo (`sticky`) al scrollear, una barra de scroll propia dibujada a mano (arrastrable, sin flechas arriba/abajo, color de marca) en vez de la nativa del navegador, columna de nombre con ancho máximo para que los nombres largos envuelvan en vez de estirar la tabla, alineación y espaciado entre columnas, y controles de paginación centrados con números circulares.<br><br>**Dos bugs encontrados y corregidos en el camino, sin relación directa con la paginación:** (1) los desplegables de `SelectMenu` (categoría/estado/cantidad por página) y el menú de acciones de cada fila (`RowMenu`) se recortaban cuando se abrían dentro de un contenedor con scroll o dentro del modal de "Nuevo producto" — ahora se renderizan con un portal a `document.body` y calculan su posición según el espacio disponible en la pantalla, igual que ya se hacía en otras partes de la app. (2) en el selector "Elegir variante" (al cambiar precio desde la fila de un producto), tanto el botón de cerrar como el fondo llamaban a la función que cierra *todo* el detalle del producto en vez de cerrar solo ese selector chico.<br><br>**Bug encontrado y NO corregido, fuera de alcance de esta tarea:** el modal de bienvenida que aparece los primeros 5 segundos después de loguearse (`WelcomeModal`) queda por encima de cualquier otro modal que se abra en esa ventana de tiempo (por ejemplo, "Nuevo producto"), bloqueando los clics hasta que se cierra solo o pasan los 5 segundos. Se reprodujo con Playwright para confirmarlo. Requeriría revisar el z-index/orden de montaje de `WelcomeModal` frente al resto de los modales de la app.<br><br>**Pruebas:** backend — 5 tests nuevos en `test_catalog.py` (catálogo completo sin parámetros, paginación con `page`/`page_size`, filtro por categoría+estado, filtro por `search`, `page_size` inválido → 422); se actualizaron los tests existentes de `test_catalog.py`/`test_imports.py` que esperaban `GET /products` como lista plana, ahora es `{items, total, page, page_size}`. Suite completa del backend: 130 tests, todos verdes. Frontend — se reescribió `ProductsPage.test.tsx` para el flujo servidor-paginado (filtro por categoría, búsqueda con debounce, números de página comprimidos con más de 10 páginas, avance con "Siguiente"); se actualizó `CategoriesPage.test.tsx` al nuevo formato de respuesta; test unitario nuevo para `buildPageList`. Suite completa del frontend: 44 tests, todos verdes. También corrí `tsc --noEmit` y `oxlint` sin errores (los warnings de `set-state-in-effect` ya existían antes en otras páginas del catálogo, mismo patrón).<br><br>**Pendiente / fuera de esta tarea:** paginación de historial de precios y de una futura pantalla de búsqueda de variantes (ya anotado arriba, para T-013 u otra tarea futura); no se tocó orden (`order_by`) en el backend; el bug de `WelcomeModal` tapando otros modales (detallado arriba) queda sin corregir.<br><br>**Corrección tras revisión (2026-09-06), 5 observaciones:** (1) `Pagination.tsx` usaba `bg-danger` (rojo) para los botones "Anterior"/"Siguiente" — no son acciones de peligro, se cambió a `bg-brand`, el mismo patrón que el resto de los botones primarios. (2) `GET /products` (T-017) ya devolvía `price_amount` por variante pero nunca se agregó al tipo `Variant` del frontend, así que la columna "Precio" de `ProductsPage` seguía hardcodeada en "Próximamente" — se agregó `price_amount: string \| null` a la interfaz y la columna ahora muestra el precio formateado (mismo `Intl.NumberFormat` que ya usa `ProductDetailPage`) cuando el producto tiene una única variante implícita con precio, "Varios precios" cuando sus variantes tienen precios distintos, y "Sin precio" cuando ninguna variante tiene precio cargado; se agregó una prueba nueva en `ProductsPage.test.tsx` que cubre los tres casos. (3) se sacó un comentario JSDoc de `paginationList.ts` (sin excepción para comentarios en el código). (4) en `index.css`, `.scrollbar-clean` tenía el color de marca hardcodeado como `rgba(175, 20, 5, …)` — se reemplazó por `color-mix(in srgb, var(--color-brand) …%, transparent)`. (5) `ruff check`/`ruff format` señalaban 4 líneas largas (E501) y falta de formateo en `test_catalog.py` — se acortaron esas líneas y se corrió `ruff format`; ambos comandos quedan limpios. Se volvió a correr `test_catalog.py` (29 tests, todos verdes) y la suite completa del frontend (45 tests, todos verdes); `tsc --noEmit` sin errores.<br><br>**Segunda corrección tras revisión (2026-09-06), 4 observaciones:** (1) `npm run typecheck`/`npm run build` fallaban: en `ProductsPage.test.tsx`, el array `products` del test de "Botones surtidos" (línea 125) no tipaba bien porque TypeScript no lograba unificar los tipos literales inferidos entre variantes con `price_amount` string y otras con `null`; se anotó ese array (y `PRODUCTS`) como `Product[]`, importando el tipo desde `../../api/types`. `npm run typecheck` y `npm run build` quedan sin errores. (2) se sacó de `backend/tests/conftest.py` el docstring de `_test_database_url` que explicaba por qué las pruebas usan una base separada (sin excepción para comentarios en el código); la explicación quedó como nota en `docs/09-propuesta-arquitectura-tecnica.md`. (3) `HighlightedText.tsx` usaba `text-danger` (rojo, color de error) para resaltar coincidencias de búsqueda, que no es un estado de error; se cambió a `bg-brand/10 text-brand`; se actualizaron las dos pruebas que esperaban `text-danger` (`HighlightedText.test.tsx` y `ProductsPage.test.tsx`), que habían quedado rotas tras el cambio de color hasta esta corrección. (4) bug serio detectado, no de esta tarea pero recién visible: en `vite.config.ts`, `API_PATH_PREFIXES` incluía `/products`, `/categories`, `/units` y `/attributes` — las mismas rutas que usa la SPA para esas pantallas —, así que el proxy de Vite (que reenvía por prefijo de path sin mirar nada más) mandaba al backend cualquier carga completa de esas páginas (F5, URL directa, link externo), y el navegador recibía el JSON crudo de la API en vez de la app; se confirmó navegando a `/products` y haciendo F5. Se corrigió agregando un `bypass` al proxy de esas cuatro rutas: si `req.headers.accept` incluye `text/html` (navegación de documento, no un `fetch()` de la propia app), Vite sirve `/index.html` en vez de proxyar; se probó F5 en `/products`, `/products/5`, `/categories`, `/units` y `/attributes`, los cinco muestran la app. Las rutas que no chocan con la SPA (`/auth`, `/accounts`, `/search`, `/variants`, `/imports`, `/health`) quedan sin bypass, igual que antes. **Pendiente:** en producción ("API y SPA se sirven desde el mismo dominio", docs/09) hace falta resolver esta misma colisión de rutas con lo que sea que sirva ahí (ese despliegue todavía no existe en el repo); no se tocó nada de eso ahora. **Revisión independiente de la madre (2026-09-06): Verificada.** Probé en vivo la colisión de rutas antes y después del fix: `curl` con `Accept: text/html` a `/products` servía el JSON crudo del backend antes del cambio, y sirve `index.html` después; con `Accept: application/json` sigue yendo al backend en ambos casos, así que el bypass no rompe los fetch reales de la app. `typecheck`, `test` (49/49) y `build` limpios; `ruff check`/`ruff format` del backend limpios. La columna Precio en `ProductsPage` ya muestra el mínimo con "desde" cuando hay variantes con precios distintos. Mergeable. |
| T-019 | Backend: permitir que cualquier usuario autenticado (no solo Administrador) consulte el nombre de la cuenta autora de un cambio de precio, para mostrar "Último cambio: hace X · por {nombre}" en el detalle de producto de la SPA | RF-018 a RF-023, RN-009 | task/T-019-price-author-visible-to-gerente | Verificada (2026-09-06) | `GET /accounts/{account_id}` hoy exige `require_role(ADMINISTRADOR)` (`backend/app/api/access.py`), pensado para la gestión de cuentas; la SPA lo reutilizó para resolver el nombre del autor de un precio (`created_by_account_id` en `Price`) y por eso una cuenta Gerente (que sí puede cambiar precios, RN-032) nunca ve el nombre, solo el propio Administrador. Decisión de la madre (2026-09-06): no abrir `GET /accounts/{account_id}` a Gerente (expondría datos de administración de cuenta que docs/02 le niega); en cambio, agregar el nombre directamente a la respuesta de precio. Revisión independiente de la madre (2026-09-06): `PriceResponse` suma `created_by_account_name`, resuelto server-side vía `prices.get_account_names` (backend/app/domain/pricing/prices.py) a partir de `created_by_account_id`, sin exponer ningún otro dato de la cuenta; aplicado en precio vigente, historial, cambio de precio y las respuestas de conflicto 409. `fetchAccount` y todo el `Promise.allSettled` de resolución de autores se eliminaron de `PricingPage.tsx` y `ProductDetailPage.tsx` (esta última no estaba en el pedido original, la tarea la encontró con el mismo problema y la corrigió también). Test nuevo `test_gerente_sees_author_name_in_price_and_history` cubre el caso exacto. Typecheck, test frontend (67/67) y build limpios; ruff check/format y test suite completa de backend (136/136) limpios. Mergeable. |
| T-020 | Backend: exponer desactivar/reactivar un valor de atributo (hoy `update_attribute_value` solo permite cambiar el texto, no el estado) | RF-032 a RF-034, RF-040, HU-09 | task/T-020-attribute-value-deactivation | Verificada (2026-09-06) | Detectado en la revisión independiente de T-012 (2026-09-05): HU-09 pide explícitamente que "desactivar un color impide usarlo en variantes nuevas", pero no existe endpoint de backend para desactivar/reactivar un `AttributeValue` (a diferencia de producto y variante, que sí lo tienen desde T-006) — así que el frontend no puede implementarlo todavía en ningún lado. Bloquea que `AttributesPage` (T-012) ofrezca esa acción sobre los valores de un atributo. Revisión independiente de la madre (2026-09-06): agrega `POST /attribute-values/{id}/deactivate` y `/reactivate` (mismo patrón que producto/variante: `require_role(GERENTE)`, csrf, scopeado por negocio vía T-028), y wirea el botón Desactivar/Activar en `AttributesPage.tsx` con `ConfirmDialog`. Tests backend cubren: desactivar/reactivar, que un valor desactivado desaparece de `list_attribute_values(active_only=True)` pero se conserva en la variante que ya lo tenía asignado, que Gerente puede y Empleado no (403), y que un valor de otro negocio da 404. Typecheck, test frontend (69/69) y build limpios; ruff check/format y test suite completa de backend (141/141) limpios. Mergeable. |
| T-021 | Backend: dejar de guardar el token de sesión en texto plano como clave primaria de `account_session` — usar su hash para la búsqueda, igual que ya se hace con la contraseña | RNF-013 (por analogía) | task/T-021-hash-session-tokens | Verificada (2026-09-05) | `core/security.py` suma `hash_token` (SHA-256 en hexadecimal) y `verify_token` (comparación en tiempo constante); no toqué `generate_session_token` ni argon2, que sigue siendo solo para contraseñas. `create_session` genera el token de sesión y el CSRF crudos, guarda únicamente sus hashes y devuelve un `IssuedSession` (la fila más los dos valores crudos), porque al hashear la fila el valor que va en la cookie deja de estar disponible en `_set_session_cookies`; las cookies siguen llevando el token crudo exactamente como antes, así que el frontend no cambia. `get_valid_session` hashea el token entrante antes del `db.get`, y `require_csrf` compara hash contra hash. Resolví el CSRF junto con la sesión como sugería esta fila: el cambio resultó simétrico y no agregó alcance. Sin migración: SHA-256 en hexadecimal ocupa exactamente 64 caracteres y las dos columnas ya eran `String(64)`; `alembic check` no detecta drift. Dos renombres de nombres que quedaban mintiendo, ninguno de los dos toca la base: las constantes `SESSION_TOKEN_MAX_LENGTH`/`CSRF_TOKEN_MAX_LENGTH` pasan a `..._HASH_LENGTH`, y el atributo `AccountSession.csrf_token` pasa a `csrf_token_hash` fijando explícitamente el nombre de columna `csrf_token`. La columna `id` la dejé con ese nombre, por consistencia con el resto de los modelos y porque sigue siendo el identificador de la fila. Archivos tocados: `core/security.py`, `constants/access.py`, `db/models/account_session.py`, `domain/access/sessions.py`, `domain/access/auth.py`, `domain/access/permissions.py`, `api/access.py`, `tests/test_auth.py`, `tests/test_security.py`. Pruebas: 3 nuevas en `test_auth.py` (que la fila guardada no contiene ninguno de los dos valores de la cookie sino sus hashes; que un token leído de la tabla, sin el valor crudo, da 401 en `GET /auth/me` — el caso pedido explícitamente; y que el CSRF leído de la tabla da 403) más 5 en `test_security.py` sobre `hash_token`/`verify_token`; adapté `test_session_expiry_extends_on_activity`, que buscaba la fila por el token crudo. Corrida completa contra PostgreSQL real: 119 pasan y 7 fallan, y esas 7 son exactamente las que ya documenta T-023 (heredan `INITIAL_BUSINESS_2_*` del `.env` local y esperan un solo negocio); fallan por el entorno, no por este cambio. `test_auth.py`, `test_security.py` y `test_accounts.py` juntos: 31/31. `ruff check` y `ruff format --check` sin observaciones. Para la revisión de la madre, dos puntos que no decido yo: (1) al desplegar esto, las sesiones ya abiertas dejan de validar (su fila guarda el token crudo, que ya no coincide con el hash del entrante), así que todos los usuarios tienen que volver a iniciar sesión una vez; (2) esas filas viejas además quedan huérfanas en la tabla, porque `get_valid_session` ya no las encuentra ni siquiera para borrarlas al vencer — con datos reales conviene un `DELETE FROM account_session` en el despliegue. Hoy la base es de desarrollo, así que no hice nada al respecto. Mensaje de commit sugerido: `fix: store only the hash of session and csrf tokens` / cuerpo: `Implements RNF-013 by analogy (T-021).` **Revisión independiente de la madre (2026-09-05): Verificada.** `hash_token`/`verify_token` correctos (SHA-256 + comparación de tiempo constante), token crudo solo viaja en la cookie, nunca se guarda. `ruff check` limpio; corrí `test_auth.py`/`test_security.py` de forma independiente (20/20), incluidos los dos casos que prueban que un token leído directo de la tabla no autentica. Coincido con la nota de la tarea sobre necesitar un `DELETE FROM account_session` en el despliegue real (rompe sesiones existentes) — no aplica todavía porque la base es de desarrollo. Mergeable. |
| T-022 | Backend: dos limpiezas menores detectadas en T-009 (2026-09-02) y nunca resueltas | D-034 | task/T-022-cleanup-t009-notes | Verificada (2026-09-05) | **Implementación (2026-09-05):** (1) `c603b2693d97_initial_schema.py` ya no importa `INITIAL_ROLES` desde `app.constants.roles`: se reemplazó por una tupla literal `("Administrador", "Gerente", "Empleado")` definida en la propia migración, igual que el resto de sus valores. No se agregó una migración nueva, se editó el archivo existente (no aplicado aún en ningún entorno). No hizo falta una prueba nueva: `test_upgrade_from_empty_database_creates_schema_and_seed_data` (`backend/tests/test_migrations.py`) ya corre la migración de punta a punta y verifica que los tres roles sembrados coincidan con `ADMINISTRADOR, GERENTE, EMPLEADO`, así que ya cubre que el cambio a literales no alteró el resultado. (2) Confirmado: no existe ningún `RuntimeError` con mensaje en español (ni en ningún idioma) en `backend/app` — se puede dar de baja esa nota, la auditoría de T-009 quedó resuelta en algún punto anterior sin actualizarla. Mensaje de commit sugerido: `fix(T-022): inline literal role names in the initial migration` / cuerpo: `Replaces the live import of INITIAL_ROLES from app.constants.roles with the three literal role names, matching the rest of the initial migration and avoiding a future change to that constant silently altering an already-applied migration (D-034). No RuntimeError with a Spanish message was found in backend/app, so that half of the T-009 note is resolved with no code change.` **Revisión independiente de la madre (2026-09-05): Verificada.** Confirmé el diff (solo la migración y esta fila). Los tres nombres literales coinciden exactamente con `INITIAL_ROLES`; correcto reusar `test_upgrade_from_empty_database_creates_schema_and_seed_data` en vez de duplicar cobertura. `ruff check` limpio. Corrí esa prueba de forma independiente: falla, pero por la causa ya registrada en T-023 (negocio duplicado por el `.env` compartido) antes de llegar siquiera a comparar los roles — no relacionado con este cambio. Confirmo también que no hay ningún `RuntimeError` en español en `backend/app`. Mergeable. |
| T-023 | Backend: aislar `test_active_business.py`/`test_migrations.py` de las variables `INITIAL_BUSINESS_2_*` del entorno en vez de depender del `.env` de quien corre la suite | — | task/T-023-isolate-business-env-tests | Verificada (2026-09-05) | Detectado en la revisión independiente de T-017 (2026-09-05): 7 pruebas (`test_me_reports_the_single_accessible_business_by_default`, `test_login_response_also_reports_active_business`, `test_account_with_access_to_two_businesses_can_switch_and_prices_stay_per_business`, `test_account_listing_is_scoped_to_the_active_business`, `test_upgrade_from_empty_database_creates_schema_and_seed_data`, `test_upgrade_from_pre_rename_schema_renames_tables_columns_and_data`, `test_upgrade_without_second_business_settings_creates_only_one_business`) asumen un escenario de un solo negocio, pero no hacen `monkeypatch` de `INITIAL_BUSINESS_2_NAME`/`INITIAL_BUSINESS_2_INDUSTRY` ni limpian el `@lru_cache` de `get_settings`, así que heredan lo que sea que tenga configurado el `.env` de quien las corre — en este entorno de desarrollo (que sí las tiene, para poder probar T-016 a mano) las 7 fallan. No es un defecto de T-016 ni de ninguna tarea posterior: es que estas pruebas no aíslan su propio entorno. No bloquea nada mientras se sepa que puede pasar; sí vale la pena resolverlo porque hoy da una falsa sensación de que hay 7 pruebas rotas en `main`. **Implementación (2026-09-05):** solo toqué las dos pruebas. En `test_active_business.py` agregué el fixture `no_second_business` (usa `monkeypatch` y se declara primero en la firma de cada prueba afectada, antes de `client`/`db_session`) y lo pedí en las 4 pruebas de la lista que viven ahí. En `test_migrations.py` agregué la función `_isolate_from_second_business_env(monkeypatch, request)` y la llamé al inicio de las 3 pruebas de la lista que viven ahí — ahí alcanza con una función normal porque esas pruebas corren la migración dentro del propio cuerpo, después de la llamada. Punto no obvio: `monkeypatch.delenv(...)` no alcanza, porque `Settings` (pydantic-settings) usa el `.env` como *fallback* cuando la variable no está en `os.environ` — quitarla del entorno del proceso no la oculta, solo hace que se lea del archivo igual. Tuve que usar `monkeypatch.setenv(..., "")` (string vacío) para pisar el valor del `.env`, que la migración semilla trata como ausente (`if not settings.initial_business_2_name`). En ambos casos limpio el `@lru_cache` de `get_settings` al entrar y al salir, así ninguna prueba deja el caché con el valor aislado para la siguiente. Corrida completa: las 12 pruebas de ambos archivos pasan (antes, 7 fallaban en este entorno, que sí tiene `INITIAL_BUSINESS_2_NAME=Despensa`); la suite completa del backend también pasa (126/126). `ruff check` y `ruff format --check` sin observaciones. Mensaje de commit sugerido: `test: isolate business-env tests from INITIAL_BUSINESS_2_* variables` / cuerpo: `Adds a monkeypatch-based fixture/helper in test_active_business.py and test_migrations.py that forces INITIAL_BUSINESS_2_NAME/INDUSTRY to an empty value and clears get_settings' lru_cache, so the 7 tests listed in T-023 no longer depend on the .env of whoever runs the suite (T-023).` **Revisión independiente de la madre (2026-09-05): Verificada.** Corrí la suite completa de backend de forma independiente: **126/126 pasan**, cero fallas (antes 111 pasaban y 7 fallaban por esto). `ruff check` limpio. Solo tocó pruebas, como correspondía. Con esto quedan cerrados los tres hallazgos de la auditoría de backend (T-021, T-022, T-023). Mergeable. |
| T-024 | Frontend: sacar Categorías/Unidades/Atributos del sidebar (D-042 revisa la decisión de T-012 de ponerlas ahí) y agregar un botón dentro de `ProductsPage` que abra un panel/modal con las tres listas para consultarlas o editarlas | HU-04, HU-09 | task/T-024-move-catalog-lists-into-products | Verificada (2026-09-06) | Decisión del Responsable (2026-09-05): la creación inline con "+" al lado de los desplegables (ya implementada en `ProductFormPage`/`VariantAttributesEditor` durante la auditoría de T-012) cubre el alta; falta el lugar para consultar/editar lo ya creado, que no debe ser una ruta propia del sidebar sino algo alcanzable desde `ProductsPage`. Las rutas `/categories`, `/units`, `/attributes` y las páginas ya existen (T-012): esta tarea es reorganizar cómo se llega a ellas, no reescribirlas. **Implementación (2026-09-05):** `navItems.ts` pierde las tres entradas de sidebar (y `NavIcon.tsx` sus glifos ya sin uso); las rutas `/categories`, `/units`, `/attributes` de `App.tsx` quedan intactas (navegación directa por URL sigue funcionando). Componente nuevo `CatalogListsModal.tsx`: un modal con pestañas (mismo patrón visual que `ConfirmDialog`) que renderiza `CategoriesPage`/`UnitsPage`/`AttributesPage` sin modificarlas — ninguna de las tres se tocó. `ProductsPage` suma el botón "Categorías, unidades y atributos" junto a "Nuevo Producto" y el estado `showCatalogLists` que abre/cierra el modal. La creación inline con "+" en los desplegables (`ProductFormPage`/`VariantAttributesEditor`) no se tocó, como pedía el Responsable. Prueba nueva en `ProductsPage.test.tsx`: abre el panel desde el botón, verifica que muestra la pestaña Categorías por defecto, cambia a Unidades y cierra el panel. Corrí toda la suite de frontend (`vitest run`): 34/34 pasan (antes 33; sumé 1). `tsc --noEmit` sin errores. No toqué el backend ni ninguna prueba backend. **Revisión independiente de la madre (2026-09-05): Verificada** (sobre la versión de esa fecha). **Ajustes de diseño posteriores a esa revisión (2026-09-06), pedidos por el Responsable sobre `CatalogListsModal.tsx` — invalidan la verificación anterior, hace falta una nueva:** (1) las pestañas dejan de ser botones con relleno de color y pasan a texto separado por líneas verticales (`divide-x`), con la activa marcada por subrayado en color marca en vez de fondo; (2) las tablas de las tres páginas reutilizadas pasan a ocupar todo el ancho del panel, neutralizando su `max-w-2xl`/`max-w-3xl` propio desde el contenedor del modal (`[&_.max-w-2xl]:max-w-none`, `[&_.max-w-3xl]:max-w-none`) sin editar esas páginas. **Primera vuelta de este ajuste cambió también el fondo detrás del panel a blanco opaco sin blur, pero el Responsable aclaró que eso no era lo pedido** (el "blanco" se refería al fondo del panel, que ya era blanco vía `bg-surface`/`#fff`, no al fondo detrás): se revirtió a `bg-ink/20 backdrop-blur-sm`, semitransparente y difuminado, igual que `ConfirmDialog`. (3) el panel pasa de altura variable (`max-h-[90vh]`, se ajustaba al contenido) a un tamaño predeterminado y fijo (`h-[85vh]`), con el área de contenido en `flex-1` y la tabla de cada pestaña estirada a ese espacio (`flex-1 overflow-y-auto`) en vez de un `max-h` arbitrario, así el scroll vertical aparece solo dentro de la tabla y solo si el contenido no entra, sin que el panel cambie de tamaño entre pestañas. Seguí sin tocar `CategoriesPage`/`UnitsPage`/`AttributesPage`: todo el ajuste vive en `CatalogListsModal.tsx`. Corrí de nuevo la suite completa (`vitest run`: 34/34) y `tsc --noEmit` (sin errores) después de estos cambios. **Reversión (2026-09-06), a pedido del Responsable:** se eliminó por completo el panel — se sacó el botón "Categorías, unidades y atributos" y su estado de `ProductsPage.tsx`, se borró `CatalogListsModal.tsx` y su prueba en `ProductsPage.test.tsx`. Consultado explícitamente, el Responsable eligió no restaurarlas en el sidebar (alternativa que se le ofreció): el resultado es que **Categorías, Unidades y Atributos quedan sin ningún punto de acceso en la SPA** (ni sidebar ni panel), solo alcanzables tipeando `/categories`, `/units` o `/attributes` directamente en la URL — las rutas y las páginas (T-012) siguen intactas, nadie las tocó. `vitest run`: 35/35 (se fue la prueba del panel). `tsc --noEmit` sin errores. Queda pendiente que el Responsable decida en algún momento cómo se llega a esas tres pantallas, o si de verdad deben quedar solo accesibles por URL directa. **Rediseño de `ProductDetailPage.tsx` (2026-09-06), del mismo commit `d5dea6b` sin documentar hasta ahora:** el formulario de edición pasó a manejar todo junto (nombre, categoría, unidad, estado activo/inactivo, precio o precio por variante) en un solo "Guardar cambios", con creación inline de categoría/unidad igual que en `ProductFormPage`. Nuevo `frontend/src/shared/CloseButton.tsx` reemplaza el botón de cerrar duplicado en `ProductFormPage`/`ProductDetailPage`/`ChangePriceModal`. En la revisión encontré que el toggle de estado desactivaba/reactivaba el producto sin confirmación (viola CU-10); el Responsable confirmó que fue un descuido, no una decisión. **Corrección (2026-09-06):** `handleSaveProduct` ahora intercepta un cambio de estado y muestra `ConfirmDialog` (mismo patrón que `ProductsPage`) antes de guardar; si se cancela, el resto del borrador (nombre/categoría/precio ya tipeados) no se pierde. De paso volvió a aparecer `text-xs`/`text-sm` en este archivo (16 veces) — vuelto a `text-base` como mínimo. 2 pruebas nuevas/reescritas en `ProductDetailPage.test.tsx`: confirma antes de desactivar, y cancelar conserva el borrador. `typecheck`, `lint` (mismas advertencias preexistentes) y `test` (36/36) corridos de forma independiente. **Revisión independiente de la madre (2026-09-06): Verificada.** Confirmé el fix de confirmación y que no queda ningún `text-xs`/`text-sm` en el archivo. Mergeable. **Corrección (2026-09-06):** el rediseño de `ProductDetailPage.tsx` (commit d5dea6b) había agregado el toggle Activo/Inactivo del formulario de edición sin ningún aviso, aplicando el cambio de estado como parte silenciosa de "Guardar cambios" — una omisión frente a CU-10, que exige explícitamente "el sistema explica el efecto y solicita confirmación" antes de desactivar/reactivar. Ahora, si al guardar el estado del borrador difiere del estado actual del producto, se muestra un `ConfirmDialog` (mismo componente y mismos textos que ya usa `ProductsPage` para desactivar/reactivar) antes de aplicar nada; si se cancela, el modal se cierra y el formulario de edición sigue abierto con el resto del borrador (nombre/categoría/unidad/precio) intacto — el guardado real (`saveProduct`) no se dispara hasta confirmar. También corregí los 16 usos de `text-xs`/`text-sm` que había vuelto a introducir ese mismo archivo, subiéndolos a `text-base` (16px), mismo criterio de accesibilidad ya aplicado ahí antes (docs/13). Prueba existente del toggle reescrita para pasar primero por el diálogo y confirmar; agregué una prueba nueva que cancela la confirmación y verifica que el nombre editado y la selección de estado no se pierden. `vitest run` de `ProductDetailPage.test.tsx`: 6/6 pasan. `tsc --noEmit` sin errores. No toqué `ProductsPage.tsx` ni `ConfirmDialog.tsx`. Mensaje de commit sugerido: `fix(T-024): confirm status change before saving product edits` / cuerpo: `ProductDetailPage's edit form applied an Activo/Inactivo change silently as part of the same save, skipping the confirmation CU-10 requires for deactivation/reactivation. Splits the save into saveProduct() and a submit handler that shows the existing ConfirmDialog pattern (already used in ProductsPage) when the draft status differs from the product's current status, only calling saveProduct() on confirm; canceling keeps the edit form and its draft intact. Also raises the 16 text-xs/text-sm instances reintroduced in this file back to text-base (16px baseline, docs/13).` |
| T-026 | Frontend: restyle del cuerpo de la vista de solo lectura de `ProductDetailPage` (grilla de info, sección de precio por variante) y de `ChangePriceModal`, según una referencia visual que pasó el Responsable | HU-01, HU-04 | main (directo, sin rama) | Verificada (2026-09-06) | Decisión del Responsable (2026-09-06): Código y Stock siguen "Próximamente" (no se adelantan Etapa 3/4). El header se mantiene igual que hoy — X para cerrar y lápiz para editar arriba, sin barra de acciones abajo ni botón de Desactivar en esta pantalla (eso sigue siendo solo del menú de fila en `ProductsPage`). Es un cambio de layout/estilo del cuerpo de la pantalla, no de datos ni de las acciones disponibles. **Implementación (2026-09-06):** la referencia visual llegó como dos capturas adjuntas directamente en esta conversación (no hizo falta pedirle el detalle al Responsable en otro lado). Solo toqué el cuerpo en modo lectura, nada del formulario de edición ni del header. En `ProductDetailPage.tsx`: la grilla de info pasa de 2 a 3 columnas (Categoría/Unidad/Variantes, esta última nueva: cuenta `product.variants.length`) con una línea divisoria antes de Descripción, dentro de la misma tarjeta gris; en el caso de precio único (sin variantes reales) el encabezado pasa de "Precios y variantes" a "Precio" y la tarjeta de precio suma la fila Stock/Último cambio y el botón "Ver historial" deshabilitado, igual que ya tenía cada variante en el caso de varias — Stock sigue mostrando "Próximamente" tal cual antes, no es dato real. En `ChangePriceModal.tsx`: "Vigente desde" pasa a color verde (`text-success`, como en la referencia) y el campo de nuevo precio suma el prefijo "$" dentro del input, mismo patrón que ya usa el precio en `ProductDetailPage`. Actualicé la única prueba que dependía del texto del encabezado viejo (`ProductDetailPage.test.tsx`, "Precios y variantes" → "Precio" en el caso de precio único). Corrí `vitest run` completo (36/36) y `tsc --noEmit` (sin errores) después de los cambios; `npm run lint` (oxlint) solo muestra las mismas advertencias preexistentes en otros archivos, ninguna nueva en los dos tocados. No hay branch `task/T-026-product-detail-restyle` creada todavía — por la regla del proyecto (esta misma sección, "Los comandos de git son siempre manuales") una conversación de IA no crea ramas aunque se lo pidan explícitamente; falta que el Responsable la cree antes de mover estos cambios ahí. Mensaje de commit sugerido: `style(T-026): restyle product detail read view and change-price modal` / cuerpo: `Adds a Variantes column and a divider before the description in ProductDetailPage's read-only info grid, renames the single-price section heading to "Precio" and adds its Stock/Último cambio row and disabled "Ver historial" button (matching the multi-variant case), and gives ChangePriceModal a green "Vigente desde" and a $ prefix on the new-price input — following the visual reference shared in this conversation (HU-01, HU-04, T-026).` **Revisión independiente de la madre (2026-09-06): Verificada.** Encontré dos cosas en la primera vuelta, ya corregidas: (1) el botón "Confirmar" de `ChangePriceModal` quedó en `bg-brand/60` con texto blanco, contraste débil (~3.35:1) contra la base de accesibilidad del proyecto — subido a `bg-brand/80` (~5.15:1, calculado con la fórmula WCAG), ya cómodo para texto grande. (2) abrir "Cambiar precio" desde el detalle ya abierto desmontaba la tarjeta de detrás (se veía la tabla de Productos un instante) — corregido para que solo se desmonte cuando el modal se abrió directo por `?changePrice=1` (ahí no hay tarjeta detrás) o al elegir variante; agregaron una prueba que confirma que el encabezado del producto sigue visible al abrir el modal desde adentro. `typecheck`, `lint` (mismas advertencias preexistentes) y `test` (36/36) corridos de forma independiente, sin errores. Mergeable (ya está en `main`, sin rama separada). |
| T-028 | Backend: mover el catálogo (producto, categoría, unidad de venta, atributo) de `organization_id` a `business_id` (D-046) | HU-?? | task/T-028-catalog-per-business | Verificada (2026-09-06) | Decisión del Responsable (2026-09-06), registrada como D-046: Mercería y Despensa no comparten catálogo en absoluto (no es "mismo producto, precio distinto", son productos distintos). Corrige D-039/docs/07. Prioridad alta: los 361 productos ya importados de Mercería deben migrarse con `business_id` de Mercería antes de que se cargue nada de Despensa. Revisión independiente de la madre (2026-09-06): producto, categoría, unidad y atributo pasan de `organization_id` a `business_id` en modelos, dominio, API de catálogo/precios/importación/búsqueda; migración `04e83accd8a1` hace backfill y revierte correctamente. Tests nuevos verifican que una categoría/unidad/atributo/producto creado en un negocio da 404 desde otro negocio de la misma organización (incluida búsqueda y precios). Typecheck, test frontend (64/64) y build limpios; ruff y test suite completa de backend (135/135) limpios. Aplicada después contra la base real (`alembic upgrade head`): los 445 productos y 361 precios de Mercería quedaron con `business_id` de Mercería, Despensa vacía, cuenta inicial con rol Dueño en ambos negocios — verificado por consulta directa. Mergeable. |
| T-027 | Backend + frontend: agregar el rol **Dueño** por encima de Administrador y redefinir el alcance de cada rol a un único negocio salvo Dueño (D-045); mueve el selector de negocio de T-025 de Administrador a Dueño exclusivamente | HU-?? | task/T-027-owner-role | Verificada (2026-09-06) | Decisión del Responsable (2026-09-06), registrada como D-045: jerarquía Empleado → Gerente → Administrador → Dueño, cada uno incluye todo lo del anterior acotado a un negocio salvo Dueño (multi-negocio). Administrar cuentas/roles e importar el catálogo se quedan en Administrador. La cuenta inicial (INITIAL_ADMIN) pasa a crearse con rol Dueño. Reemplaza D-044 y la sección de docs/02 que igualaba Administrador con "dueño del negocio" (ya actualizada). **Implementación (2026-09-06):** `app/constants/roles.py` reemplaza las listas planas de `require_role` por una jerarquía (`ROLE_HIERARCHY`/`ROLE_RANK`); `require_role` ahora toma un único "rol mínimo" y acepta cualquier rol de rango igual o superior, en vez de una lista explícita — actualicé todos los call sites (`access.py`, `catalog.py`, `pricing.py`, `imports.py`); catálogo/precios bajan a `require_role(GERENTE)` (Administrador y Dueño quedan incluidos por jerarquía), cuentas/importación se quedan en `require_role(ADMINISTRADOR)`, y `POST /auth/active-business` sube a `require_role(DUENO)`. Migración nueva `d1e2a9f47b6c` agrega el rol Dueño y promueve la cuenta inicial (`INITIAL_ADMIN_USERNAME`) de Administrador a Dueño en todos sus negocios. Agregué `backend/scripts/seed_dev_accounts.py` (fuera de Alembic a propósito, para no alterar los tests de migraciones ni instalaciones reales) que crea `empd`/`empm`/`gerd`/`germ`/`admind`/`adminm` (uno por rol y negocio) y una única cuenta `due` (Dueño, con acceso a ambos negocios), contraseña `1234` para todas — el mínimo de contraseña bajó de 8 a 4 caracteres (`PASSWORD_MIN_LENGTH`) para permitirlo, a pedido explícito del Responsable. En el frontend, `features/access/roles.ts` reemplaza `isAdministrador` por `isDueno` (gatea `BusinessSelectorPage`/`ProtectedRoute` y el selector de negocio de `AccountMenu`) y agrega `canViewDashboard` (Administrador y Dueño); `canManageCatalog` suma Dueño al set existente. El Panel (`/`) ahora depende de `canViewDashboard`: `App.tsx` agrega `HomeRoute`, que redirige a `/products` si el rol no puede verlo, y `Sidebar` (que ahora recibe `account` por prop en vez de leerlo de `useAuth`, para poder testearlo sin `AuthProvider`) oculta el ítem "Panel" del mismo modo. No hay formulario de alta de cuentas en el frontend (no existe todavía, sigue siendo solo backend/tests), así que no hay un desplegable de rol que actualizar ahí. Until ahora sin pantalla propia: no hay UI nueva de "elegir negocio" más allá de lo que ya hizo T-025, solo cambia a quién se le muestra. Pruebas nuevas/actualizadas: `test_permissions.py` (jerarquía de rangos), `test_active_business.py` (Administrador con acceso a dos negocios ahora recibe 403 al cambiar, Dueño puede), `test_accounts.py` (alta de cuenta con rol Dueño), `test_migrations.py`/`test_auth.py` (la cuenta inicial queda como Dueño), `Sidebar.test.tsx` (oculta Panel para Gerente) y `App.test.tsx` (selector de negocio pasa a Dueño; agregué el caso de Administrador que ya no lo ve). Corrí backend (`ruff check` limpio, `pytest`: 134/134) y frontend (`vitest run`: 60/60, `tsc -b --noEmit` sin errores, `oxlint` sin warnings nuevos, `npm run build` sin errores) de forma independiente. **Revisión independiente de la madre (2026-09-06): Verificada.** Confirmé que la jerarquía de `require_role` cubre correctamente todos los call sites (cuentas/importación en Administrador, catálogo/precios en Gerente, cambio de negocio en Dueño), que la migración promueve bien la cuenta inicial y revierte, y que el Panel/selector de negocio quedan gateados como se pidió. Encontré `PASSWORD_MIN_LENGTH` bajado a 4 y pregunté directamente al Responsable si era intencional: confirmó que sí, por ahora — registrado aparte como D-047 para que quede trazado como decisión y no como descuido. Repetí `typecheck`, `test` (64/64 en mi corrida) y `build` de frontend, y `ruff check`/`ruff format --check`/`pytest` completo (134/134) de backend, todo limpio. Mergeable. |
| T-029 | Despliegue del MVP: hosting gratuito alcanzable por internet, con HTTPS (DP-005, PT-011) | PT-011 | — (commit directo en `main`, sin rama, hecho por la madre) | Verificada (2026-09-06) | Resuelve DP-005: se eligió Render (free, corre el backend FastAPI y sirve el build de la SPA desde el mismo dominio) + Supabase (free, solo la base PostgreSQL, persistente a diferencia de la de Render). El Responsable pidió armar esto por primera vez y explícitamente que lo hiciera la madre directamente (no como tarea aparte), guiándola paso a paso para lo que solo ella podía hacer (crear las cuentas, pegar las claves). Se agregó `backend/app/main.py` (middleware que sirve `index.html` para cualquier navegación de browser, `Accept: text/html`, resolviendo para producción la misma colisión de rutas SPA/API que T-018 resolvió en el proxy de desarrollo — probado localmente con Docker antes de confiar en el Dockerfile), `Dockerfile` (build en dos etapas, SPA + backend), `render.yaml`, `.dockerignore` y `docs/14-despliegue.md` con la guía completa. Verificado con un build y corrida completa de Docker local contra una base descartable (migraciones desde cero, login, cuenta inicial con rol Dueño) antes de desplegar de verdad. Ya en producción: se corrió `alembic upgrade head` contra la base real y se migraron los 445 productos y 361 precios de Mercería desde la base local a Supabase (categorías/unidades ya coincidían en id con los valores de atributo color precargados, solo hubo que copiar producto/variante/precio con sus ids explícitos y reasignar los campos de auditoría a la única cuenta existente en el entorno nuevo). Se detectó y corrigió que el servicio de Render había quedado en la región Oregon mientras Supabase está en Ohio (latencia cruzada de costa a costa); se recreó el servicio en la región correcta (`casa-diaco.onrender.com`). Queda como limitación conocida y aceptada del plan gratuito: el servicio se duerme tras 15 minutos sin uso (tarda unos segundos en responder la primera vez) y ocasionalmente el enrutador de Render devuelve un 404 "no-server" transitorio incluso con el servicio activo — no es un bug del código, es inherente a no pagar por una instancia siempre activa; la alternativa es el plan pago (~USD 7/mes) si esto resulta molesto en el uso diario. |
| T-030 | SPA: pantalla de alta y gestión de cuentas de usuario (HU-06, CU-03, docs/02) — hasta ahora solo se podía por API/tests | HU-06, CU-03 | task/T-030-accounts-screen | Verificada (2026-09-06) | Backend ya existía completo desde T-002 (crear, listar, editar, desactivar/reactivar, resetear contraseña); faltaba la pantalla en la SPA. La madre pasó una captura de referencia (mockup genérico "User management") para el layout de tabla, adaptada a lo que el backend realmente tiene (sin email/avatar/last-active inventados). Revisión independiente de la madre (2026-09-06): pantalla `/cuentas` con tabla (nombre con iniciales, usuario, rol en badge de color, negocios en badges, estado), filtros por rol/negocio/estado, alta y edición por modal, restablecer contraseña (D-028, sin correo), desactivar/reactivar con `ConfirmDialog`; gateada a Administrador+ tanto en el nav (`navItems.ts`) como en la ruta (`AccountsRoute` redirige a `/products`). Colores nuevos de rol/negocio agregados como tokens en `index.css` (no hardcodeados). De yapa, el backend ahora deja que Dueño vea cuentas de todos sus negocios accesibles en vez de solo el activo (bien testeado: `test_dueno_account_listing_spans_all_accessible_businesses`). Encontré una duplicación real: `features/accounts/RowMenu.tsx` era una copia idéntica de `features/catalog/RowMenu.tsx`; pedí consolidarlo en `shared/RowMenu.tsx` y se aplicó correctamente, con ambos duplicados borrados y los imports actualizados. Typecheck, test (87/87) y build de frontend limpios; ruff y test suite completa de backend (142/142) limpios. Mergeable. |
| T-031 | Frontend: hacer responsive las pantallas de tabla (Productos, Precios, Cuentas) para celular y tablet | RN-024 (accesibilidad, docs/06) | task/T-031-responsive-tables | Verificada (2026-09-07) | El Responsable reportó (2026-09-07), con capturas de celular y tablet reales, que las tres pantallas de tabla son "0 responsive": en celular, la fila de filtros (buscador + 3-4 selects + "Limpiar búsqueda") se apila a ancho completo dentro de un contenedor de altura fija (`h-[calc(100svh-4rem)]`), consumiendo casi toda la altura visible antes de que la tabla llegue a mostrar ninguna fila. En tablet, el sidebar no colapsa a hamburguesa (el breakpoint `md:` en `AppLayout.tsx` ya se cruzó) pero el ancho de contenido que queda no alcanza para la tabla (`min-w-[900px]`/`min-w-[1000px]` en `ProductsPage`/`PricingPage`/`AccountsPage`), y en una de las capturas de `PricingPage` en tablet la tabla se ve directamente rota (sin filas visibles, con la barra de scroll superpuesta). Afecta a las tres pantallas por igual porque comparten el mismo patrón de filtros en `flex flex-wrap` y tabla con `min-w` fijo. El Responsable aclaró (2026-09-07) que el estilo de los filtros debe ser distinto entre celular y escritorio (no el mismo layout apilado escalado hacia abajo) — la fila de filtros no es una sola cosa que "se achica", el diseño en celular puede/debe ser una solución distinta (por ejemplo un panel de filtros colapsable, o controles agrupados distinto), no una versión reducida del layout de escritorio: el motivo es que ocupan demasiado espacio vertical en celular/tablet, no solo estética. Además pidió cambiar el ícono de hamburguesa (`☰`, `AppLayout.tsx` línea ~35) por otro ícono, mismo mecanismo (sigue abriendo el mismo panel lateral de navegación); pidió mantener el ícono SVG que se implementó, no hace falta cambiarlo de nuevo. Revisión independiente de la madre (2026-09-07): en las tres pantallas, vista de tarjetas por defecto (`ViewToggle`, evita el scroll horizontal en vez de forzarlo) con toggle a tabla disponible; filtros pasan a un botón "Filtros" que abre `FiltersSheet` (hoja inferior) por debajo de `lg`, visibles en fila como antes en `lg` y superior — solución realmente distinta, no una versión apilada del layout de escritorio; paginación usa un selector "Página X / Y" en mobile en vez de la fila de botones. Encontré `frontend/src/shared/FieldRow.tsx` creado pero sin usar en ningún lado (duplicación real: `AccountsPage`/`PricingPage` repetían a mano el mismo patrón "etiqueta + valor" en sus tarjetas); pedí que se resolviera y se aplicó correctamente, usándolo donde correspondía (`ProductsPage` lo dejó afuera con motivo: su sección Precio/Unidad/Variantes es una grilla de 3 columnas, un patrón distinto, no duplicación). Typecheck, test (87/87) y build limpios en ambas rondas. Mergeable. |
| T-035 | Backend + frontend: imagen opcional por producto, subida a Supabase Storage (D-049) | HU-?? | task/T-035-product-images | Verificada (2026-09-07) | El Responsable pidió (2026-09-07) agregar fotos a los productos. Una imagen por producto (no por variante), opcional, guardada en Supabase Storage (no en el disco de Render, que no es persistente). Revisión independiente de la madre (2026-09-07): backend sube/borra el archivo en Supabase Storage vía httpx puro (sin SDK), valida tipo (jpg/png/webp) y tamaño (5MB), scopeado por negocio; tests mockean la llamada HTTP real. Migración agrega `product.image_url`. Frontend agrega selector de imagen con drag-and-drop y confirmación (D-048) en tres variantes (alta, edición con staging, reemplazo inmediato), miniaturas en tabla/tarjetas de Productos, imagen en la ficha de detalle. Encontré un bug real en la primera entrega: `saveProduct()` en `ProductDetailPage.tsx` agregó un `close()` que navegaba a la lista después de CUALQUIER edición (no solo la imagen), sin que el Responsable lo hubiera pedido — un test se había reescrito para aceptar esa regresión en vez de detectarla. Pedí que se sacara; el Responsable aclaró que en realidad prefiere que Guardar Y Cancelar naveguen a la lista de Productos (no que se quede en la ficha) — se agregó `close()` también al botón Cancelar, con test nuevo que lo confirma. De yapa, se sumó un scrollbar personalizado a la vista de tarjetas de Productos/Precios/Cuentas y al modal de detalle (`useScrollbar.ts`, variante de `useTableScrollbar` sin encabezado fijo), consistente con el resto de la app. Typecheck, test (100/100) y build de frontend limpios; ruff check/format y test suite completa de backend (147/147) limpios. Mergeable. |
| T-034 | Frontend: unificar buscador y filtros en una sola fila en escritorio (Productos, Precios, Cuentas) | RN-024 | task/T-034-fix-filters-row-layout | Verificada (2026-09-07) | El buscador había quedado en un div separado de los selects de filtro (T-033), por lo que en escritorio se veían en dos filas en vez de una. Revisión independiente de la madre (2026-09-07): las tres pantallas ahora comparten un único contenedor `flex flex-col gap-3 lg:flex-row lg:flex-wrap lg:items-center` con el buscador (extraído a `frontend/src/shared/SearchInput.tsx`, con su propia "×" para limpiar visible solo en mobile) seguido de los selects y "Limpiar búsqueda" en la misma fila para `lg:` y superior; en mobile, el buscador queda solo con un grid de 2 columnas para "Filtros"/"+ Nuevo…" debajo. Probado en 1024px y 1280px: todo en una sola fila. Typecheck, test (94/94) y build limpios. Mergeable. |
| T-036 | Frontend: correcciones puntuales de diseño exclusivas de celular/tablet (Productos, Precios, Cuentas, alta de producto) | RN-024 | task/T-036-mobile-tablet-fixes | Verificada (2026-09-08) | El Responsable pidió 10 ajustes puntuales tras usar la app en celular/tablet real: ViewToggle en la misma fila que el título; en Precios, reemplazar el botón "Filtros" (no tenía sentido, el único filtro real era "por página") por el select directo; agregar el ícono de lupa a `SearchInput.tsx` (compartido, también se ve en escritorio); en Productos, reposicionar el ícono de "Filtros" al principio del botón; en Cuentas, unificar los colores de las etiquetas de rol/negocio entre tabla y tarjetas (se borró `businessBadgeClasses`, ya sin uso); en el alta de producto, separar/diferenciar el ícono de "quitar variante" del de "quitar valor" (ahora un `TrashIcon` reusado de `shared/icons.tsx`); ajustar tamaño de tarjeta y foto en el campo de imagen para mobile (`ProductImageField.tsx`, con `lg:` preservando el tamaño de escritorio); no mostrar decimales cuando el precio termina en ",00" (nuevo `shared/formatPrice.ts`, consolidó 4 formatters de precio duplicados); mostrar solo el primer nombre del autor en el historial de precios. Revisión independiente de la madre (2026-09-08): 9 de 10 correctamente aplicados y verificados uno por uno contra el pedido original; faltaba agrandar el "+" del botón "Nuevo Producto" en mobile — el Responsable decidió no hacerlo, el texto acortado a "Nuevo Prod." ya alcanza. Dos efectos secundarios inevitables en escritorio, aceptados por el Responsable: el ícono de lupa se ve también en desktop (componente compartido) y el reacomodo del modal de alta de producto (que no tiene layouts separados por tamaño de pantalla) también se ve ahí. Typecheck, test (100/100) y build limpios. Mergeable. |
| T-037 | Frontend: botón flotante "volver arriba" | RN-024 | task/T-037-scroll-to-top | Verificada (2026-09-08) | El Responsable pidió un botón fijo abajo a la derecha que aparece solo con scroll y sube al principio de la página. Revisión independiente de la madre (2026-09-08): `ScrollToTopButton.tsx`, montado una sola vez en `AppLayout.tsx`, usa un listener en fase de captura sobre `window` — cubre tanto el scroll de la página como el de los contenedores internos con scroll propio (las tablas de escritorio con `lg:overflow-hidden`, T-031), más completo de lo pedido explícitamente. Encontré un archivo de depuración sin borrar (`_debug2.test.ts`); se eliminó. El color gris (`bg-line`) fue una decisión explícita del Responsable, no un pendiente. Typecheck, test (104/104) y build limpios. Mergeable. |
| T-038 | Backend: modelo y API de proveedores y faltantes (Etapa 2, D-050) | RF-041 a RF-047, RN-033 a RN-039, CU-11 a CU-13 | task/T-038-suppliers-and-shortages | Verificada (2026-09-11) | Arranca la Etapa 2. Proveedor: CRUD scopeado por negocio (mismo patrón que categoría/unidad/atributo, T-028), con razón social, contacto, email, teléfono, última compra y categorías que provee (relación muchos-a-muchos con categoría); un producto tiene a lo sumo un proveedor habitual (columna opcional en `product`). Faltante: se marca sobre la variante (no el producto); cualquier cuenta autenticada puede crear uno (`get_current_user`, sin `require_role`); estados Faltante → Pedido → Recibido con transiciones válidas (RN-038) y sin duplicar un faltante abierto por variante (RN-039); endpoint de listado con filtro/agrupación por proveedor o categoría y por estado (para la lista de pendientes, RF-044) y un endpoint liviano de conteo para el contador de navegación (RF-046). Administrar proveedores y asignar el proveedor habitual quedan reservados a Gerente y superiores (`require_role(GERENTE)`); marcar/gestionar un faltante no requiere ese permiso. **Implementación:** modelos `Provider`/`Shortage`/`provider_category` y columna `product.provider_id` (migración `f1a3c7d9b2e4`); servicios `app/domain/catalog/providers.py` y `shortages.py`; endpoints nuevos en `app/api/catalog.py`; tests en `tests/test_providers.py` y `tests/test_shortages.py`. RN-039 reforzada también con índice único parcial en `shortage`. Sin pendientes de alcance conocidos. **Revisión independiente de la madre (2026-09-11):** repasé modelos, migración (constraints y el índice único parcial que refuerza RN-039), servicios de dominio (la máquina de estados de `ALLOWED_TRANSITIONS` coincide exactamente con RN-038), endpoints y permisos (Gerente+ para administrar proveedores, cualquier cuenta para faltantes, CSRF en todo lo que muta) — todo coherente con el alcance acordado, sin nada fuera de lo pedido. `alembic upgrade head` aplicó limpio y `pytest` 183/183 (26 pruebas nuevas). Mergeable. Commit sugerido: `feat(T-038): add suppliers and shortages backend`. |
| T-039 | Frontend: pantallas de proveedores y faltantes (Etapa 2, depende de T-038) | HU-11 a HU-14 | — | Pendiente | Pantalla de Proveedores (alta/edición/desactivación, mismo patrón de tabla+tarjetas responsive que Productos/Precios/Cuentas, T-031, con confirmación antes de crear/editar/desactivar, D-048) y de Faltantes (lista agrupable por proveedor o categoría, con las acciones de avanzar/cancelar estado). Acción de "marcar como faltante" accesible desde donde tenga sentido en ProductsPage/ProductDetailPage (menú de fila de variante, por ejemplo). Contador de faltantes pendientes visible en la navegación (Sidebar), gateado a ningún rol en particular (RF-043: cualquier cuenta). El Responsable pasó un mockup con una pestaña "Órdenes de compra" (productos con cantidad, precio de compra y subtotal por línea, estado Pendiente/Parcial) — se acordó (2026-09-08) que eso es Etapa 3 (compras, ligado a inventario/ventas, docs/07 "extensiones futuras"), no Etapa 2. La pantalla de Proveedores igual debe tener esa segunda pestaña ya visible ahora, con el mismo placeholder "En construcción" que ya usa `DashboardPage.tsx` (ícono + "Próximamente vas a ver acá..."), sin funcionalidad real todavía — mismo patrón que "Inventario"/"Ventas" en el sidebar. |
| T-040 | Backend: modelo y API de clientes y fiados (Etapa 2, D-051) | RF-048 a RF-051, RN-040 a RN-043, CU-14, CU-15 | — | Pendiente | Cliente: CRUD scopeado por negocio (mismo patrón que proveedor, T-038), nombre y teléfono opcional. Fiado: movimiento de cuenta corriente (tipo cargo/pago, importe, sin desglose de productos), creado por cualquier cuenta autenticada (`get_current_user`, sin `require_role`, igual que el faltante). Endpoint de saldo por cliente (suma de cargos menos pagos, RN-042, no editable directo) y de lista de clientes con saldo pendiente (RF-051). |
| T-042 | Frontend: activar el link "¿Problemas para acceder?" del login | D-028 | task/T-042-login-help-modal | Verificada (2026-09-10) | Reemplacé el `<a href="#">` muerto por un botón que abre `LoginHelpDialog.tsx` (mismo patrón visual que `ConfirmDialog`, solo botón "Entendido"), con el mensaje pedido sobre D-028. Revisado por la madre: diff limitado a `LoginPage.tsx` y el nuevo `LoginHelpDialog.tsx` (el resto del working tree ya estaba modificado desde antes, sin relación con esta tarea); mensaje exacto al pedido, texto "Administrador o Dueño" verificado contra `canManageAccounts` en `roles.ts`. `npm run typecheck`, `npm run lint` (mismas 13 advertencias preexistentes, ninguna nueva) y `npm run build` sin errores; no hay pruebas de `LoginPage` que esta tarea pudiera romper. Mensaje de commit sugerido: `feat(T-042): add login help modal for password reset guidance`. |
| T-043 | Frontend: sistema de notificaciones tipo popup (toast) para errores y confirmaciones de éxito | — | task/T-043-toast-notifications | Verificada (2026-09-10) | Tercera ronda sobre esta tarea. Primero: rediseño del popup a pedido del Responsable (círculo de ícono sobre el borde superior, título "¡Listo!"/"Error", centrado en pantalla, fondo difuminado no bloqueante, sin botón de acción) y `shared/LoadErrorCard.tsx` nuevo (misma identidad, con "Reintentar") para errores de conexión/400/500 al cargar una pantalla completa — ver ronda anterior para el detalle pantalla por pantalla. Después, el Responsable pidió expandirlo a **toda** creación/modificación y **todo** error del sistema, no solo los puntos originales. Antes de tocar código armé un plan (aprobado) con un inventario completo: 27 acciones de escritura en 13 archivos. Regla aplicada: éxito → `showSuccess(...)`; error → `showError(...)` reemplazando el `role="alert"` inline que hoy lo muestra. Alcance: crear/editar categoría, unidad, atributo y valor de atributo, activar/desactivar valor; activar/desactivar producto; cambiar precio (variante y "todas las variantes", en `ChangePriceModal` y en el flujo propio de `PricingPage`); crear categoría/unidad al vuelo, editar producto, editar variante, activar/desactivar variante, crear atributo/valor al vuelo (`ProductDetailPage`, `VariantAttributesEditor`); crear cuenta, editar cuenta, restablecer contraseña, activar/desactivar cuenta (`AccountsPage`); error de login (`LoginPage`); cerrar sesión y cambiar de negocio (`AccountMenu`); y un aviso nuevo que no existía antes — sesión expirada (401 en un pedido de fondo), agregado en `AuthContext.tsx`. Excepción a la regla, encontrada probando: los errores embebidos en la descripción de un `ConfirmDialog` para pedir "reconfirmá con el dato actualizado" (conflicto 409 de precio en `PricingPage`, activar/desactivar valor de atributo o variante) los dejé **solo** en el diálogo, sin sumar un popup en paralelo — mi plan original decía agregarlo también ahí, pero un test lo detectó: el popup se renderiza por encima del `ConfirmDialog` (z-index) y repite el mismo texto, tapando el diálogo que el usuario todavía necesita para confirmar de nuevo. Corregido antes de cerrar. También extraje `shared/PriceInput.tsx` (el input con "$" fijo que ya existía duplicado dentro de `PricingPage.tsx`) para reusarlo en el precio inicial del alta de producto. `npx tsc -b --noEmit`, `npm run lint` (15 advertencias, sin nuevas) y `npx vite build` limpios; `npx vitest run --run`: 114/115 — la única falla es el flake ya documentado de `ProductsPage.test.tsx` (debounce de búsqueda), pasa siempre en corrida aislada, no relacionado. Actualicé ~13 archivos de test para envolver con `ToastProvider` y sumé aserciones de éxito representativas. **Revisión independiente de la madre (2026-09-10): Necesita ajustes (solo documentación, no código).** Repasé el diff completo de esta tercera ronda contra `main`: el patrón de migración es consistente en las ~13 pantallas (éxito → `showSuccess`, error → `showError`, con la excepción de los conflictos 409 embebidos en `ConfirmDialog` correctamente respetada y verificada caso por caso: `PricingPage`, `AttributesPage` — `statusChangeError` — y `ProductDetailPage` — `variantStatusError` — de verdad quedan solo en el diálogo, no duplicados en un popup). `LoadErrorCard`/`PriceInput` bien extraídos y reutilizados. Señalé que `ProductFormPage.tsx` dejó de importar y usar `ProductImageField` en el paso "Precio inicial" (el mensaje de error de subida de imagen perdió la frase "Podés reintentarlo acá abajo") sin que el resumen de esta fila lo mencionara — el Responsable confirmó (2026-09-10) que pidió esa eliminación directamente, así que el código está bien; lo que faltó fue nombrarlo en este resumen. Dejo constancia acá para que quede registrado: si falla la subida de imagen al crear un producto, hoy no hay forma de reintentarla sin ir después a `ProductDetailPage`. Corrí de forma independiente `npm run typecheck` (limpio), `npm run lint` (15 advertencias, mismos tipos que antes, ninguna nueva), `npm run test -- --run` (115/115, sin el flake de `ProductsPage.test.tsx` esta vez) y `npm run build` (limpio); el único aviso en consola durante los tests (clave React duplicada `3` en "creates a new account" de `AccountsPage.test.tsx`) es preexistente, no de esta ronda. Mergeable. Commit sugerido: `feat(T-043): add toast notifications across the SPA and load-error retry cards`.

Cuarta ronda, dos ajustes chicos: (1) la X del popup pasó a ser el mismo `shared/CloseButton.tsx` que usa cualquier modal de la app (antes era un botón propio más chico) — ajusté `Toast.test.tsx` porque cambia el `aria-label` ("Cerrar" en vez de "Cerrar notificación") y el cierre ahora tiene el mismo delay de 150ms que todo `CloseButton`; (2) el breadcrumb del paso "Precio inicial" en `ProductFormPage.tsx` pasa a mostrar el nombre del producto recién creado en el medio (`Catálogo › {nombre} › Precio inicial`, último segmento en rojo), volviendo al patrón de 3 segmentos que ya usa `ProductDetailPage.tsx` — el paso "Nuevo producto" (antes de crear, sin nombre todavía) se queda en 2 segmentos. `npx tsc -b --noEmit`, `npm run lint` (15, sin nuevas), `npx vitest run --run` (115/115 esta corrida) y `npx vite build` limpios.

Quinta ronda: el Responsable pidió que los mensajes de éxito/error se vean más grandes, con la accesibilidad +50 años ya documentada como motivo explícito. Agrandé `shared/Toast.tsx` (tarjeta `max-w-sm`→`max-w-md`, círculo de ícono de 48px a 64px, título `text-xl`→`text-3xl`, mensaje `text-base`→`text-xl`) y `shared/LoadErrorCard.tsx` en la misma proporción (comparten identidad visual, habría quedado inconsistente dejar uno grande y el otro chico) incluido el botón "Reintentar" (`h-12`/`text-base` → `h-14`/`text-lg`). Actualicé docs/13 para dejar registrado que el tamaño es a propósito, no solo estético. `npx tsc -b --noEmit`, `npm run lint` (15, sin nuevas), `npx vitest run --run` (115/115) y `npx vite build` limpios — ningún test dependía de las clases de tamaño. Commit sugerido (nada se commiteó todavía, reemplaza el de rondas anteriores): `feat(T-043): add toast notifications for every create/update action and error`. Verificar (frontend): `npm run typecheck`, `npm run lint`, `npm run test -- --run`, `npm run build`. |
| T-041 | Frontend: pantalla de Clientes/Fiados (Etapa 2, depende de T-040) | HU-15, HU-16 | — | Pendiente | Pantalla de Clientes (alta/edición, mismo patrón responsive de T-031, confirmación antes de crear/registrar movimiento, D-048), con el saldo visible por cliente y el detalle de cargos/pagos al entrar a uno. Lista de clientes con saldo pendiente (HU-16). |
| T-044 | Frontend + backend: auditar qué campos son obligatorios en cada formulario de alta/edición de toda la SPA, y hacerlo cumplir; auditar además dónde debería impedirse el nombre duplicado | RF-017, D-046, D-047 | task/T-044-required-fields-and-duplicates | Verificada (2026-09-11) | Relevamiento (docs 03/07/08/09) y plan confirmado por el Responsable (2026-09-10): categoría/unidad/atributo/valor de atributo ya bloqueaban duplicados de antes (constraint en base, por negocio) y no requirieron cambios; producto/variante ahora bloquea el duplicado exacto **dentro del mismo producto** (mismo nombre de variante y mismos atributos), y sigue solo advirtiendo entre productos distintos (RF-017). Contraseña "segura" = se mantiene el mínimo de 4 de D-047 (no se revierte) sumando una regla aparte (mayúscula + minúscula + número), sin exigir cambio a contraseñas ya existentes; nombre de cuenta pasa a ser obligatorio también en el backend. Backend: `app/domain/access/accounts.py` (+`errors.py`, `api/access.py`), `app/domain/catalog/products.py` (+`errors.py`, `api/catalog.py`); tests nuevos en `tests/test_accounts.py` y `tests/test_catalog.py`, y actualicé la contraseña de fixture a una que cumpla la regla nueva en los 6 archivos de test que la comparten. Frontend: `features/accounts/AccountsPage.tsx` (validación + pista de contraseña segura, con test nuevo), `features/catalog/ProductFormPage.tsx` (bloquea variantes indistinguibles antes de enviar) y `features/catalog/ProductDetailPage.tsx` (nombre de producto obligatorio al editar, y corregí un bug real que encontré de paso: el guardado de producto trataba cualquier 409 como conflicto de precio, lo que iba a tapar el mensaje del nuevo bloqueo de variante duplicada). Nada quedó pendiente del alcance acordado. Además, a pedido directo del Responsable (2026-09-10, no en el plan original): agregué un botón "Ver historial" de precios por variante en `ProductDetailPage.tsx` (reutiliza `fetchVariantPriceHistory`, ya usado en `PricingPage`), y cambié `shared/PriceInput.tsx` de `type="number"` a `type="text"` con saneamiento manual de caracteres (rechaza letras con un mensaje propio en vez de depender del comportamiento del input nativo). **Revisión independiente de la madre (2026-09-10):** repasé el diff completo backend+frontend; el patrón de validación (touched/attemptedSubmit, asterisco en campos obligatorios, checklist de contraseña en vivo, chequeo de nombre duplicado contra el servidor antes de confirmar) es consistente entre `AccountsPage`, `ProductFormPage` y `ProductDetailPage`; `normalizeForComparison.ts` replica exactamente la lógica de `app/core/text.py` (NFKD + descartar diacríticos + minúsculas); la extensión del bloqueo de nombre duplicado a la importación CSV (`api/imports.py`) es coherente con el alcance. Las dos adiciones sin plan original (historial de precios, `PriceInput` a texto saneado) las confirmó el Responsable como pedido directo suyo. Sin objeciones de código. Verificación independiente: `npm run typecheck`/`build` limpios, `npm run lint` con las mismas 15 advertencias de siempre (ninguna nueva), `npm run test -- --run` 129/129 (14 nuevas), `pytest` 157/157. Mergeable. Commit sugerido: `feat(T-044): require secure passwords, account names, and block exact duplicate variants within a product`. |

Segunda ronda (2026-09-11), ajustes del Responsable sobre el formulario de cuentas: el mínimo de caracteres del usuario ahora se muestra como pista (y como error si no se cumple); los cuatro campos (Nombre, Usuario, Contraseña, Rol) llevan asterisco de obligatorio; y cada campo valida al salir de él (sin esperar el submit), marcando el borde en rojo y mostrando el mensaje puntual — mismo tratamiento en crear cuenta, editar cuenta y restablecer contraseña (el "no coinciden" de repetir contraseña se sigue mostrando en el momento, como ya hacía). Solo `features/accounts/AccountsPage.tsx` cambió, con tests nuevos en `AccountsPage.test.tsx`; también actualicé ahí los `getByLabelText` existentes con una regex tolerante al asterisco (mismo patrón que ya usaba `ProductDetailPage.test.tsx`). Commit sugerido: `feat(T-044): add inline field validation to the account forms`. Verificar frontend: `cd frontend && npm run typecheck && npm run lint && npm run test -- --run && npm run build`.

Tercera ronda (2026-09-11), mismo tratamiento para la sección de Productos y un pedido nuevo sobre precios. (1) `ProductFormPage.tsx` (crear producto): Nombre, Categoría y Unidad ya tenían asterisco; ahora cada uno valida al perder el foco (el nombre por blur del input, categoría/unidad por blur del `SelectMenu`, que aprendió `hasError`/`onBlur` para esto) marcando el borde en rojo y mostrando el mensaje puntual, sin esperar el submit. (2) `ProductDetailPage.tsx` (editar producto): mismo tratamiento para Nombre (Categoría/Unidad no lo necesitan al editar porque un producto existente siempre llega con un valor válido). (3) Precio — el pedido concreto del Responsable: escribir una letra en un campo de precio no hacía nada visible porque el input era `type="number"` (el navegador descarta la tecla en silencio) y no se entendía por qué. Reescribí `shared/PriceInput.tsx` como `type="text"` con saneamiento propio (deja pasar dígitos y un solo punto decimal) y un mensaje "Solo se permiten números." que aparece en el momento y desaparece solo al tipear un carácter válido; lo reutilicé en los tres lugares que todavía tenían su propio `<input type="number">` duplicado (`ChangePriceModal.tsx`, y los dos precios de `ProductDetailPage.tsx` — precio único y precio por variante), que antes no pasaban por el componente compartido. De paso, `ChangePriceModal.tsx` también exige precio > 0 en el cliente (antes dependía del `min` nativo del input numérico, que se perdió al cambiar de tipo). Tests nuevos: `shared/PriceInput.test.tsx` (nuevo), y casos de campo obligatorio en `ProductFormPage.test.tsx`/`ProductDetailPage.test.tsx`; también les adapté los `getByLabelText` de "Nombre" con la misma regex tolerante al asterisco. Archivos: `shared/PriceInput.tsx`, `shared/SelectMenu.tsx`, `features/catalog/ProductFormPage.tsx`, `features/catalog/ProductDetailPage.tsx`, `features/catalog/ChangePriceModal.tsx`, y sus tests. Commit sugerido: `feat(T-044): add inline field validation to product forms and reject non-numeric price input`. Verificar frontend: `cd frontend && npm run typecheck && npm run lint && npm run test -- --run && npm run build`.

Cuarta ronda (2026-09-11): el Responsable reportó que la ventana "Cambiar precio" se rompía al abrirla desde Productos — bug mío de la ronda anterior, me olvidé de importar `PriceInput` en `ChangePriceModal.tsx` (quedaba usado en el JSX pero sin `import`, `ReferenceError` apenas montaba). Corregido. Aproveché para revisar opcional/obligatorio en el resto del formulario de editar producto: "Imagen" decía "(opcional)" en el alta pero no en editar (`StagedProductImageField`, ahora dice lo mismo en los dos lugares); "Precio" en editar tenía un asterisco de obligatorio heredado de antes de esta tarea que no correspondía — el código siempre dejó el precio vacío como "no tocar el precio actual", nunca bloqueó guardar por eso — lo cambié a "(opcional)" en el precio único y agregué la misma aclaración en la columna "Precio" de la tabla de variantes. Nada de esto tenía test que lo cubriera específicamente porque son solo textos de ayuda, no validación. Archivos: `features/catalog/ChangePriceModal.tsx`, `features/catalog/ProductDetailPage.tsx`, `features/catalog/ProductImageField.tsx`. Commit sugerido: `fix(T-044): fix missing PriceInput import and correct optional/required labels`. Verificar frontend: `cd frontend && npm run typecheck && npm run lint && npm run test -- --run && npm run build`.

Quinta ronda (2026-09-11), dos pedidos más del Responsable. (1) El botón "Ver historial" en `ProductDetailPage.tsx` (editar producto) estaba deshabilitado a propósito desde que se construyó esa pantalla — nunca se implementó el diálogo, a diferencia de `PricingPage.tsx` que ya tenía uno funcionando contra `GET /variants/{id}/prices`. Porté ese mismo diálogo (lista ordenada por fecha, con importe, quién lo cambió y desde/hasta) a `ProductDetailPage.tsx` y conecté los dos botones (precio único y por variante) a `fetchVariantPriceHistory`, con su propio `useScrollbar`. Test nuevo en `ProductDetailPage.test.tsx`. (2) En la vista de tarjetas de `/products`, un producto sin foto mostraba una caja gris grande con un ícono — pedido explícito de sacarla. Ahora, si `image_url` es `null`, la tarjeta no reserva ese espacio: la insignia Activo/Inactivo (antes superpuesta sobre la imagen) pasa a un renglón propio alineado a la derecha. La vista de tabla (ícono chico en la celda) no se tocó, solo se pidió sacarlo de la tarjeta. Test actualizado en `ProductsPage.test.tsx`. Archivos: `features/catalog/ProductDetailPage.tsx`, `features/catalog/ProductsPage.tsx`, y sus tests. Commit sugerido: `feat(T-044): show product price history and hide the empty image placeholder in product cards`. Verificar frontend: `cd frontend && npm run typecheck && npm run lint && npm run test -- --run && npm run build`.

Sexta ronda (2026-09-11), la más grande de esta tarea. (1) El Responsable pidió revertir el punto 2 de la ronda anterior: mejor mostrar siempre la imagen (o el gris) en la tarjeta, como estaba antes — revertido `ProductsPage.tsx`/`.test.tsx` a su versión previa, sin cambios netos ahí. (2) Reviso un caso que trajo el Responsable ("prueba1" contra "variante #451, precio $0.00"): no es un bug — `DuplicateWarning.tsx` no muestra precio; el "$0.00" era el placeholder del campo de precio inicial del formulario, no el precio de la variante parecida. Se lo expliqué, sin tocar código ahí. (3) Cambio de fondo pedido explícitamente: nombre de producto pasa a ser único por negocio (no por categoría) y **bloqueante**, no solo advertencia — "no se puede crear un producto con dos nombres por más que sea distinta categoría". Agregué `_check_duplicate_product_name` en `products.py` (mismo patrón que categoría/unidad/atributo, sin constraint en base todavía — lo puedo sumar con una migración si lo quieren más adelante) en `create_product` y `update_product`, error nuevo `DuplicateProductName` → 409 "Ya existe un producto con ese nombre.". Esto vuelve inalcanzable el camino de `find_possible_duplicates` para nombres iguales entre productos distintos (ya no pueden coexistir) — lo dejé como está, sin removerlo, para no tocar de más; aviso que quedó como código muerto en ese caso puntual por si quieren que lo saque en otra ronda. Actualicé/reemplacé dos tests de `test_catalog.py` y `test_imports.py` que asumían que ese estado (dos productos con el mismo nombre) podía existir, y también agregué un catch de `DuplicateProductName` en el endpoint de confirmar importación (`api/imports.py`) para que ese caso límite (fila de importación que crearía un nombre repetido en otra categoría) devuelva 409 en vez de romper con un 500. (4) El pedido más importante, aplicado también a Cuentas: "el mensaje de error debería mostrarse ANTES que el de confirmar... confirmar se muestra cuando está todo bien, sin excepción". Antes, un nombre de usuario o de producto duplicado solo se descubría después de aceptar el diálogo de confirmación (fallaba recién contra el backend). Ahora: en Cuentas, el usuario se valida al instante contra la lista de cuentas ya cargada (sin ir al servidor, `existingUserNames` nuevo en `AccountFormModal`); en Productos, al hacer clic en "Guardar" se dispara una verificación contra `GET /products?search=...` (con normalización de acentos/mayúsculas, `shared/normalizeForComparison.ts` nuevo) *antes* de abrir el diálogo de confirmación — si hay duplicado, el diálogo nunca aparece, se ve el error en el campo y el botón queda en "Verificando…" mientras tanto. (Nota de diseño: probé primero una verificación en vivo mientras se tipea con debounce, pero la descarté por el riesgo de que el fetch adicional interfiriera con el orden de llamadas mockeadas de los tests existentes — la verificación puntual al hacer clic en Guardar es más simple y determinística, y cumple igual el pedido de "antes del confirmar, sin excepción"). Actualicé ~10 tests existentes en `ProductFormPage.test.tsx`/`ProductDetailPage.test.tsx` que ahora necesitan un mock extra para esa llamada, y sumé tests nuevos para el bloqueo en Cuentas y Productos (alta y edición). Confirmé además que ninguno de estos cambios está duplicado por breakpoint ni por vista cards/tabla: los formularios de alta/edición son un solo componente cada uno (no hay versión mobile separada), y el fix de precios vive en el componente compartido `PriceInput`, que ya se usa en las 4 instancias de `PricingPage` (cards y tabla) más `ChangePriceModal` y `ProductDetailPage`. Archivos: `backend/app/domain/catalog/{products,errors}.py`, `backend/app/api/{catalog,imports}.py`, `backend/tests/{test_catalog,test_imports}.py`; `frontend/src/shared/normalizeForComparison.ts` (nuevo), `frontend/src/features/accounts/{AccountsPage.tsx,AccountsPage.test.tsx}`, `frontend/src/features/catalog/{ProductFormPage,ProductDetailPage}.tsx` y sus tests, `frontend/src/features/catalog/ProductsPage.tsx`/`.test.tsx` (revertidos). Commit sugerido: `feat(T-044): block duplicate product names business-wide and validate uniqueness before confirming`. Verificar backend: `cd backend && pytest`. Verificar frontend: `cd frontend && npm run typecheck && npm run lint && npm run test -- --run && npm run build`.

Séptima ronda (2026-09-11): el Responsable corrió los tests y encontró lo que a mí se me pasó — `ProductsPage.test.tsx` tiene un test de integración ("reflects a product edited from the detail modal in the table") que monta `ProductDetailPage` anidado y le cambia el nombre al producto; con la verificación de nombre duplicado de la ronda anterior, ese test necesitaba un mock extra igual que los que ya había actualizado en `ProductDetailPage.test.tsx`, y se me pasó por estar en un archivo distinto. Backend 157/157 y build limpios (confirmado por el Responsable); las otras 2 fallas del frontend eran el flake de debounce ya documentado — de hecho una de las dos (el debounce) ya pasó sola en la corrida aislada, confirmando que no era mío. Solo faltaba esta. Agregado el mock que faltaba en `ProductsPage.test.tsx`. Commit sugerido: `test(T-044): add the missing duplicate-name check mock in ProductsPage's integration test`. Verificar frontend: `cd frontend && npm run test -- --run`.

Octava ronda (2026-09-11): el Responsable pidió unificar el estilo de "(opcional)" que ya usaba `ProductDetailPage.tsx` (gris, sin mayúsculas aunque la etiqueta esté en mayúsculas) en todos los lugares de la SPA que lo necesiten. Encontré 2 de los 7 usos con formato distinto: "Imagen (opcional)" al crear producto (`NewProductImagePicker` en `ProductImageField.tsx`) era texto plano sin resaltar, y "Nombre (opcional)" de la variante en `ProductFormPage.tsx` usaba una clase parecida pero no igual (`font-normal opacity-60` en vez de `font-normal normal-case opacity-70`). Ya quedaron los 7 con la misma clase exacta. Sin impacto en tests (ninguno busca ese texto literal). Commit sugerido: `fix(T-044): unify the "(opcional)" label style across the SPA`. Verificar frontend: `cd frontend && npm run typecheck && npm run lint && npm run test -- --run && npm run build`. |
| T-032 | Frontend: agregar `ConfirmDialog` antes de crear, editar o desactivar/reactivar cualquier dato en toda la SPA (D-048) | RN-024 | task/T-032-confirm-before-writes | Verificada (2026-09-07) | El Responsable pidió (2026-09-07) que toda acción de escritura tenga confirmación explícita, como ya tiene el selector de negocio y las desactivaciones — incluyendo crear, no solo modificar. Alcance: `ProductFormPage` (crear producto), `ProductDetailPage` (editar producto y variantes — el toggle de estado ya confirma, T-024), `CategoriesPage`/`UnitsPage`/`AttributesPage` (crear/editar categoría, unidad, atributo, valor de atributo — desactivar/reactivar valor de atributo ya confirma, T-020), `AccountsPage` (crear/editar cuenta — desactivar/reactivar y restablecer contraseña ya confirman, T-030). `PricingPage` (T-013) y las desactivaciones de producto/variante (T-014/T-024) ya confirman, no tocar. Revisión independiente de la madre (2026-09-07): cada formulario intercepta el submit y muestra `ConfirmDialog` antes de aplicar el cambio (describiendo qué se crea, o "de X a Y" en las ediciones); cancelar conserva el borrador sin aplicar nada; el cambio de estado de producto sigue con su propio diálogo separado (T-024), sin duplicarse con el nuevo de edición general. El Responsable también movió a mano los botones "+ Nuevo Producto"/"+ Nueva cuenta" (visibles junto al título en escritorio, junto al botón "Filtros" en mobile, con `lg:hidden`/`hidden lg:flex`) — revisado, consistente entre ambas pantallas. Typecheck, test (94/94, suma `UnitsPage.test.tsx` nuevo) y build limpios. Mergeable. |

El orden sigue el propuesto en [06-historias-y-aceptacion.md](06-historias-y-aceptacion.md#orden-sugerido-del-mvp), salvo T-009, que depende de que T-002 esté mergeada y puede intercalarse cuando convenga.
No es una división rígida: la madre puede partir una fila en tareas más chicas
si conviene, siempre que cada una siga siendo verificable de forma
independiente.
